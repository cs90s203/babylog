# 寶寶日誌 — 版本記錄

版號規則跟 JPL（日語學習 App）主 dev 對話一致：`MAJOR.MINOR.PATCH`——新功能升 MINOR、
架構級的重大改動升 MAJOR、bug 修正/小改善升 PATCH。版本號顯示在設定頁最下方。

## v2.37.0 — 2026-08-25

**iPhone「加入主畫面」自訂圖示**

- 原因：iOS Safari 的「加入主畫面」完全不讀 `manifest.webmanifest`，只認
  `<link rel="apple-touch-icon">`，而且必須是真的點陣 PNG（原本用的是 SVG data-URI
  favicon，iOS 讀不到，於是退回抓網頁截圖當圖示，看起來就像通用的 web/emoji 縮圖）。
- 新增 `icons/` 目錄，內含沿用原本橘底 👶 設計（`#F0A500` 圓角方形）產出的多尺寸
  PNG（32/152/167/180/192/512/1024），`index.html` 加上對應的 `apple-touch-icon`
  link tag；`manifest.webmanifest` 的 icons 也一併換成真正的 PNG（原本的 100×100
  SVG 對 Android/桌面安裝的圖示品質也不夠）。
- 已安裝到主畫面的舊捷徑不會自動換圖示——iOS 只在「加入主畫面」當下讀取一次，
  需要先移除舊的再重新加入才會套用新圖示。

## v2.36.2 — 2026-08-20

**把 snowy5420@gmail.com 加入 friendA 家庭**

- 前端白名單（`FAMILIES.friendA`）與 `firestore.rules` 的 `isFriendAFamily()` 同步
  加入（一律小寫，配合 `.lower()` 正規化比對）。
- **需要在 Firebase Console 重新發布規則才會生效**——repo 裡的 `firestore.rules`
  只是版本控制的副本，不會自動部署（2.33.6 那次就是漏了這步，這次不要再漏）。
- snowy5420 原本已綁定 default 家庭的裝置**不會**跳出選家庭畫面（本機已有有效偏好，
  照舊直接進 default），要看 friendA 時用「設定 → 切換寶寶」切換即可。
- 實測驗證：`familyIdsForEmail('snowy5420@gmail.com')` 正確回傳
  `["default","friendA"]`，大小寫變體（Snowy5420@Gmail.com）解析結果一致，
  其他成員的家庭歸屬不受影響。

## v2.36.1 — 2026-08-20

**修復：「同步狀態確認中」永遠不會變成已連線——伺服器確認事件根本沒被送達**

- 你回報：資料同步完全正常（default 家庭兩支手機互通無誤），但設定頁的同步狀態
  一直停在「同步狀態確認中…」，從來不會變成「✓ 即時同步中」。
- 根因（機制確定，不是推測）：Firestore 監聽器**預設不通知「只有中繼資料變化」的
  事件**。App 打開時第一個快照從本機快取來（`fromCache: true`，此時顯示「確認中」
  是正確的）；幾秒後伺服器確認完成、`fromCache` 翻成 `false`——但因為資料內容跟
  快取完全一樣（安靜時段沒有新紀錄），這個「確認完成」對 Firestore 來說只是中繼
  資料變化，**預設模式下不會再發快照通知**。程式碼永遠等不到那個訊號，指示就永遠
  卡在「確認中」，只有剛好有新紀錄進來時才會被動清掉。這也回頭解釋了 2.33.5 以來
  「卡在離線快取」偵測的一部分誤報：不是連線卡住，是「已連上」的通知從來沒送到。
- 修法：三個監聽器（events／growth／settings）加上 `includeMetadataChanges: true`，
  讓伺服器確認的那一刻真的發出通知。`docChanges()` 不帶參數呼叫時仍然只回傳真實
  資料變化，合併與推送邏輯完全不受影響；中繼資料事件是純客戶端狀態，不會多算
  任何 Firestore 讀取用量。
- 實測驗證（模擬 Firestore）：三個監聽器都正確收到 `includeMetadataChanges: true`
  參數且回呼參數位置正確；重現你回報的確切情境——快取載入後顯示「確認中」→
  伺服器發出「資料相同、fromCache=false」的中繼資料事件 → 指示正確清除變綠，
  全程**不需要任何新資料**；中繼資料事件不造成任何資料變動；兩台裝置同步模擬
  11 個情境全數通過無回歸。真實裝置上的最終確認需要在你的手機上更新後觀察：
  打開 App 後幾秒內，設定頁應顯示「✓ 即時同步中」。

## v2.36.0 — 2026-08-19

**新增：多家庭帳號第一次登入要明確選家庭，不再靜默猜測**

- 起因：`lunamamahappy@gmail.com`（同時是 `default` 跟 `friendA` 兩個家庭的成員，
  實際身分是兩家共用的月嫂）回報「登記的內容沒有反應到其他帳號」。追查後發現
  真正原因是 Firestore 規則還是舊的、大小寫敏感版本（見下方另一則修復），但過程中
  同時發現一個獨立、原本就存在的設計漏洞：**同時屬於多個家庭的帳號，第一次在
  某支裝置登入、本機還沒有「上次用哪個家庭」的紀錄時，程式碼會靜靜選
  `FAMILIES` 名單裡排第一個的家庭當預設，完全不會提示使用者「你現在在看哪個
  寶寶」**——對只屬於單一家庭的帳號（絕大多數使用者）完全沒差，但像月嫂這種
  同時服務多個家庭的角色，會在自己毫不知情的狀況下被綁到錯的寶寶。
- 修法：這種情境現在會先跳出一個**必須選擇**的畫面——「這支手機要看哪個寶寶？」
  ，用寶寶的**真實姓名跟大頭貼/emoji**列出每個選項（不是內部的 `default`／
  `friendA` 這種代號），選了之後才會開始同步、記住這支裝置之後的偏好。已經綁定
  過的裝置（例如 cs90s203 自己一直在用的手機）完全不受影響，不會突然跳出這個
  畫面——只有「本機完全沒有偏好紀錄」或「偏好記錄的家庭這個帳號已經不屬於」
  這兩種情況才會觸發。
- 實測驗證：多家庭+無偏好 → 正確進入選擇畫面、不猜測；多家庭+有效偏好 →
  照舊直接解析（既有裝置行為不受影響）；偏好失效（換過家庭名單）→ 正確退回選擇
  畫面而非亂猜；單一家庭帳號 → 永遠不會跳出選擇畫面。畫面截圖確認選項正確顯示
  寶寶姓名與 emoji，點選後正確綁定、畫面正常消失並繼續原本流程；兩台裝置同步
  模擬 11 個情境重新跑過全數通過。
- **這不是 lunamamahappy 回報問題的根因**——那則是規則沒有真的發布最新版（見
  上一則對話紀錄），這則是同一次調查中額外發現、值得先修掉的獨立設計漏洞。

## v2.35.1 — 2026-08-19

**睡眠自動停止門檻改成 10 小時**（原本 18 小時）

- 實測驗證：確認計時超過 10 小時會正確自動停止並記錄。

## v2.35.0 — 2026-08-19

**新增：入睡／醒來計時按鈕，記錄實際睡眠**

- 首頁新增一顆「😴 入睡」浮動按鈕，位置鏡射哺乳 R 按鈕（貼著中間「+」按鈕的內側）。
  點一下開始計時，再點同一顆結束並記錄——跟哺乳計時器完全同一套機制：本機計時、
  跨重新整理/關閉 App 仍會續跑、超過安全上限（睡眠設 18 小時，哺乳是 1.5 小時）
  自動停止並記錄，避免忘記按導致計時器一直跑。
- 這是**單軌計時**，不像哺乳有左右可切換——設計上刻意簡化成一顆按鈕：入睡中這顆
  按鈕會變藍色、秀出經過時間、文字改成「醒來」；再點一次結束計時、按鈕變回灰色
  「入睡」。
- 長按按鈕（計時未啟動時）可以補記之前忘了按的睡眠，時長輸入用「時＋分」（哺乳是
  「分＋秒」，因為睡眠常常是好幾個小時）。
- 睡眠紀錄會出現在活動時間軸上（😴 圖示 + 時長），可以點開編輯時長/時間/照顧者，
  或刪除；編輯畫面也有「▶ 繼續計時」可以接續一筆記錄重新開始計時。
- 「下載備份 JSON」與「匯出 Google 日曆 CSV」都正確支援睡眠紀錄的格式與時長換算。
- **這次不做跨裝置即時同步**：入睡計時中，只有正在計時的那支手機看得到、能停止；
  跟哺乳計時器現有的架構一致（計時狀態是裝置本機資料，不會同步，只有計時結束、
  真正存成一筆紀錄後才會同步到其他裝置）。
- **既有的睡眠推估功能完全沒動**：統計頁「睡眠時數 推估」跟紀錄頁「前一晚睡眠
  (推估)」還是照舊用「前一餐到後一餐」的間隔去估算，這次新增的是額外、獨立的
  真實紀錄，不是取代或整合推估功能。
- 實測驗證：完整跑過開始/停止計時（含按鈕顏色、文字、經過時間即時更新）、記錄寫入
  時長正確、時間軸顯示、編輯（時長轉換 3600/60 正確）、刪除確認對話框文字正確、
  長按補記、繼續計時接續舊紀錄、太短（<60秒）不記錄、超過 18 小時自動停止、CSV
  匯出格式，共 8 組測試全數通過，也確認沒有影響到既有的成長建議卡功能。

## v2.34.2 — 2026-08-02

**成長建議改成逐月內容，不再用 2 個月一個區間**

- 你回報「2 個月的寶寶不該顯示 2–4 個月的建議」，要求細到當月。原本的區間是我
  自己整理的，資料密度不夠支撐逐月內容，這次改用背景研究流程，實際查證 CDC
  「Learn the Signs. Act Early.」發展檢核表（官方檢查點：2、4、6、9、12、15、18、
  24、30、36 個月）、AAP Bright Futures 健兒門診指引、WHO 副食品添加建議，針對
  0–24 個月**每一個月**都整理出各自的發展重點跟注意事項，不再共用同一份內容。
- 24 個月以後，因為官方參考資料本身就只到 30、36 個月這兩個檢查點（不是我省略
  不做），超過 24 個月會顯示最接近的已達成檢查點內容，並附註「目前沒有每個月都
  不同的細分內容，顯示最接近的 OO 參考內容」，不會假裝內容是專門為那個月寫的。
- 成長總結卡的年齡標題格式統一：滿 12 個月以後改顯示「X歲」或「X歲Y個月」（例如
  滿 2 歲整顯示「2歲」，不是「2歲0個月」），跟原本設定頁的年齡顯示邏輯一致。
- 實測驗證：2 個月大寶寶（你回報的原始案例）確認顯示「2個月」專屬內容，不再是
  「2–4個月」；0 天、12 個月整、24 個月整都對應到各自的精確月齡；26、32、40 個月
  這種落在官方檢查點之間的年齡，正確顯示實際年齡標題（例如「2歲2個月」），內容
  則附註標明借用最近的檢查點（24／30／36 個月）。

## v2.34.1 — 2026-08-02

**修正 2.34.0 自己的月齡分界 bug，年份挪到月曆正上方，成長建議加上注意事項**

- 你回報：2 個多月的寶寶，卻顯示「1–2 個月」的建議，應該要是「2–4 個月」。檢查
  發現 [views.js](js/views.js) 的區間判斷用 `ageMonths <= maxMonths`，導致剛滿
  2 個月那天還是卡在「1–2 個月」（因為 2 <= 2 成立），要滿 3 個月才會跳到下一區間
  ——跟標題「1–2 個月」代表「滿 1 個月到未滿 2 個月」的原意對不上。改成
  `ageMonths < maxMonths`（嚴格小於），滿 2 個月的當下就會正確跳到「2–4 個月」。
  用「剛滿 1/2/4 個月」「未滿 1 個月的最後一天」等邊界值都重新測過，確認跟標題
  的意思一致了。
- 年份位置：從「紀錄」標題正下方，移到月曆正上方、跟「📊比較」按鈕同一列的左側，
  框外但緊貼著月曆，不再跟月曆隔了兩張卡片的距離。
- 成長建議卡新增「⚠️該注意的事」區塊，跟原本的發展重點分開列，內容是常見的居家
  安全/照護注意事項（例如仰睡、換尿布台不離人、副食品噎到風險、爬行後的居家防護
  等），跟「這個階段開始會做什麼」的正向發展重點區隔開來。

## v2.34.0 — 2026-08-02

**紀錄頁：年份移出月曆、新增成長總結與成長建議**

- 年份改顯示在「紀錄」標題下方、月曆框外的左上角，月曆本體只留月份，不用每次看
  月曆都重複看到年份。
- 新增**成長總結卡**（月曆上方）：目前幾歲幾個月幾天、從第一筆成長紀錄到最新一筆
  的體重/身高變化量（只有 1 筆成長紀錄時不顯示變化量，避免顯示 +0.0 這種沒有意義
  的數字）。
- 新增**成長建議卡**（月曆上方）：依目前月齡對照到對應的發展階段（0–1、1–2、2–4、
  4–6、6–9、9–12、12–18、18–24 個月、2 歲以上），列出常見的發展重點——追視、翻身、
  副食品時機、睡過夜、精細動作等，文字刻意保持「多數」「開始」「約」這種彈性語氣，
  提醒這是參考範圍不是硬性時間表。有身高體重紀錄時，額外附上目前的 WHO 成長曲線
  百分位（跟統計頁「成長曲線」分頁用的是同一份 LMS 對照資料與計算邏輯，兩邊數字
  不會兜不起來）。
- 這兩張卡片的內容是靜態整理好的一般衛教常識，不是即時查詢外部資料；純粹是個人
  參考功能，跟醫療診斷、生長曲線判讀無關。
- 沒有填寫寶寶生日的家庭，這兩張卡片會自動不顯示，不影響原本的畫面。
- 實測驗證：完整跑過「沒有生日」「只有 1 筆成長紀錄」「剛出生」「2.5 歲」四種邊界
  情境，卡片顯示/隱藏與內容都符合預期；正常情境（5.5 個月、2 筆成長紀錄）截圖確認
  版面正確、百分位數字跟成長曲線頁一致。

## v2.33.20 — 2026-08-02

**拿掉自動重新整理、把離線警示降級——這個訊號本身不夠準，不該再驅動嚇人的動作**

- 你實測證實：即使畫面顯示「無法連接伺服器」，兩支手機的紀錄其實還是正常互通。
  這代表 `metadata.fromCache` 這個訊號本來就無法做到 100% 準確（Firestore SDK
  的已知行為），繼續把整套自動重新整理、紅色警示都建立在它上面，只會不斷產生
  假警報——今天光是被這個訊號誤導觸發的重新整理，就貢獻了不少額度消耗。
- 修法：不再嘗試把這個訊號修到完全準，改成降低它出錯時的代價：
  1. **拿掉「卡住 12 秒自動整頁重新整理」**——這個機制的前提（卡住=真的斷線）
     已經證實不成立，自動重整只會在誤報時白白重讀一次全部資料、浪費額度。
  2. **拿掉「重整過仍卡住」的額外警示層**（`networkLikelyBlocked`）——沒有自動
     重整了，這個「重整後還是不行」的狀態也就不需要存在。
  3. **手動的「🔄 重新連線伺服器」按鈕保留**，真的需要時使用者還是能自己按，但
     文字改成中性語氣（「同步狀態確認中」），不再用篤定的紅色「無法連接伺服器」
     字樣——我們已經證實這句話有時候是錯的。
  4. 診斷資訊面板的說明文字也一併改成如實反映：`fromCacheOnly` 為 true 時註記
     「可能只是還沒收到能證明連線的新資料，不一定代表真的斷線」。
- 實測驗證：確認拿掉自動重整後，即使 `fromCacheOnly` 卡住一段時間也不會觸發
  `location.reload()`；`networkLikelyBlocked` 這個屬性已完全移除、不會殘留；
  原本「收到新資料就解除警示」「detach 時清除卡住狀態」這兩個既有行為都還正常
  運作；兩台裝置同步模擬 11 個情境全數通過。

## v2.33.19 — 2026-08-02

**修正 2.33.18 自己的漏洞：判斷條件依賴的全域狀態，剛好是卡住時永遠到不了的狀態**

- 2.33.18 上線後你回報還是一樣卡住。檢查發現那個修法本身有邏輯漏洞：判斷「是否已經
  過了初始載入階段」用的是 `this.state === "done"`——但 `"done"` 要等三個監聽
  （events／growth／settings）**全部**都至少收到一次回應才會標記，如果裝置本來就
  卡住、其中一個監聽從來沒有機會回應，狀態永遠到不了 `"done"`，我加的這個保護機制
  就永遠不會生效，等於在它最需要發揮作用的情境下自動失效。
- 修法：不再依賴這個全域狀態，改成每個監聽自己記錄「我自己是不是已經收過第一次
  回應」——只要事件監聽自己收到過第一次回應之後，又收到本機沒有的新資料，就直接
  證明連線是通的，不用等其他兩個監聽也跟著就緒。
- 實測驗證：模擬「settings 監聽從來沒有回應、整體狀態卡在初始值」的情境下，事件
  監聽仍然能靠自己收到的新資料正確解除離線警示；同時確認全新一輪連線的「第一次」
  快照不會被誤判成已連線；兩台裝置同步模擬 11 個情境全數通過。
- 如果重新整理後還是卡住，麻煩測試時**同時請另一支手機新增一筆紀錄**——這個修法
  要等「真的收到一筆本機沒有的新資料」才會生效，單純重新整理、沒有新資料進來的話，
  就算連線其實是通的，這次的判斷也還沒有東西可以拿來確認。

## v2.33.18 — 2026-08-02

**真正的癥結：不是連線壞了，是「顯示離線」這個判斷本身會誤報**

- 決定性的一句話：你回報「兩支手機實際做紀錄，紀錄有正常互相推送過去」，但畫面
  同時間還在顯示「無法連接伺服器」。如果紀錄真的能即時互推，連線邏輯上不可能是
  斷的——這代表今天追的根本不是「連線壞掉」，而是**我們自己用來判斷「是否離線」
  的邏輯本身會誤報**，把明明正常的連線誤判成離線，之前好幾版的修復方向都因此
  繞了遠路。
- 根因：程式用 Firestore 回傳的 `metadata.fromCache` 當作唯一的健康判斷依據——
  這個欄位有記錄在案的已知行為（firebase-js-sdk#8343）：**即使連線真的在正常
  收送即時更新，這個欄位也可能一直卡在 `true`，不會如預期般在連線恢復時自動
  變回 `false`**。也就是說，這個欄位從一開始就不是 100% 可靠的「是否真的離線」
  指標，我們卻把整套自動重整、警示畫面都建立在它上面。
- 修法：不再只單方面相信 `metadata.fromCache`，改成同時看**實際有沒有真的收到
  新資料**——如果某一筆監聽在「已經完成過一次初始載入之後」，收到了本機原本
  沒有的新紀錄或更新（例如另一支手機剛新增的紀錄），這個事實本身就足以證明
  連線是通的，不管 `fromCache` 怎麼講，直接把「離線中」的警示解除。初始載入
  階段（第一次連上、還在讀本機快取）不受影響，不會誤判成「已連線」。
- 實測驗證：模擬「已完成初始載入、標記為離線中，這時收到另一裝置的新紀錄」→
  確認離線警示正確解除；模擬「收到的快照沒有任何實際變化」→ 確認不會誤判成
  已連線、真正離線的狀態不會被誤解除；重新跑過兩台裝置同步模擬 11 個情境，
  全數通過，沒有回歸。
- **這個版本先不要在正式帳號上測試**——目前讀取額度已經到 4.5萬／5萬（90%），
  再測下去會把額度燒光，而且額度吃緊本身也可能讓連線變慢/不穩，會干擾判斷這次
  修復到底有沒有真的解決問題。等額度重置、或決定升級 Blaze 方案之後，才有辦法
  乾淨地驗證。

## v2.33.17 — 2026-08-02

**撤回 2.33.14 的 30 天同步視窗——這才是真正卡住離線快取的原因**

- 2.33.16 上線後，你回報連私密瀏覽（完全乾淨、沒有任何本機資料的環境）都開始卡住了，
  而且抓到一個關鍵時序：**剛登入時顯示 OK、資料正常匯入，但一重新整理就卡在離線
  快取**。私密瀏覽是全新環境，不可能是「本機資料卡住」——這代表問題出在剛登入
  跟重新整理之間，程式碼走的路徑不一樣。
- 真正根因：2.33.14 把即時監聽加上 `.where("updatedAt", ">=", cutoff)` 篩選條件
  （只監聽最近 30 天），同時新增一個「裝置第一次綁定時，用一次性讀取抓完整歷史」
  的機制。這個帶篩選條件的即時監聽從一開始就連不上真正的伺服器；只是第一次登入時，
  那個「一次性完整讀取」（走的是普通讀取，不是即時監聽）剛好把資料正確撈回來，
  畫面看起來一切正常。重新整理後，一次性讀取的旗標已經記過不會再做，只剩下那個
  本來就有問題的篩選式監聽在跑，問題才真正浮現。
- 這個改動從來沒有機會用真正的 Firestore 帳號測試過——今天所有的自動化測試
  （`tools/twodevice-sync-test.html`）都是接一個假的雲端模擬，不會真的呼叫
  Firestore 的 `.where()` 查詢，這個關鍵路徑完全沒被驗證到就上線了。
- 修法：整個撤回 2.33.14 的改動，即時監聽退回原本沒有篩選條件、已經穩定跑了好幾週
  的版本；一次性完整讀取機制一併移除（沒有視窗限制就不需要它）。30 天視窗這個省
  讀取量的優化之後要重新來，且要先找到能驗證真正 Firestore 查詢行為的測試方式，
  不能再憑推論上線。
- 2.33.16 的 SDK 升級（9.23.0 → 12.17.0）予以保留——目前沒有證據指向它是這次問題
  的成因，而且它對應的是一個有官方紀錄佐證的已知 bug，先保留觀察。
- 實測驗證：撤回後開發環境重新初始化正常、無錯誤；兩台裝置同步模擬 11 個情境
  全數通過。真正的即時監聽連線是否恢復，要等你實機測試才能確認。

## v2.33.16 — 2026-08-02

**真正找到卡在離線快取的根因：SDK 版本太舊，踩到官方已修的多分頁 bug**

- 前一版（2.33.15）的 `experimentalAutoDetectLongPolling` 沒有解決問題——你的手機
  裝上之後重新整理，還是一樣卡在離線快取。
- 關鍵測試：請你用 Safari **私密瀏覽**（全新、完全沒有本機資料的乾淨環境）開
  同一個網址、登入同一個帳號——**連線正常**。這證實問題不是網路、不是帳號、不是
  SDK 連線協定，而是**這支手機主要瀏覽環境裡，本機已經卡住的持久化狀態**本身出了
  問題。
- 查到根因：這個 App 釘死使用的 Firebase SDK 版本是 **9.23.0**（2023 年 6 月釋出）。
  Firebase 官方在 **2024 年 9 月**修過一個已知 bug（firebase-js-sdk#8343）：多分頁
  情境下（這個 App 用的 `enablePersistence({synchronizeTabs: true})` 正是這個功能），
  `metadata.fromCache` 可能會卡在 `true` 永遠不恢復，即使伺服器端資料其實已經同步
  好了。我們的 SDK 版本比這個修復早了一年多，完全符合症狀——不是我們自己寫的
  程式碼邏輯有問題，是引用的 SDK 版本太舊，踩到 Google 自己都已經修掉的 bug。
- 修法：把 Firebase SDK 從 9.23.0 升級到目前最新穩定版 **12.17.0**。App 用的是
  「compat」相容層 API（`firebase.firestore()`、`.collection()`、`.doc()`、
  `.batch()` 這種舊式寫法），Google 特別維護這層相容 API 讓舊寫法能持續跨大版本
  正常運作，所以這次升級不需要改寫任何呼叫方式。
- 實測驗證：升級後開發環境重新初始化正常（無錯誤、無 persistenceError）；重新跑過
  兩台裝置同步模擬的全部 11 個情境（雙向新增/編輯/刪除、離線補送、衝突解決、拒絕
  過期覆蓋），全部通過，確認沒有回歸。
- 這次沒辦法在開發環境重現「卡在離線快取」本身（需要真的踩到那個多分頁時序問題），
  所以還是要等你那支手機實際測試才能最終確認解決——但這次的根因判斷有官方 GitHub
  issue 的紀錄佐證，加上私密瀏覽能連線這個直接證據支持，不是憑空猜測。

## v2.33.15 — 2026-08-02

**修復：某些網路環境下，即時同步永遠卡在「離線快取」**

- 症狀：你的手機切過 WiFi、切過 5G 都一樣連不上伺服器，登出重登也沒用，但一般網頁
  瀏覽完全正常。
- 排查過程：先確認 Firebase 官方沒有服務中斷、額度沒用盡、`firestore.rules` 沒有
  漏發布問題（過程中確實抓到 2.33.6 那次規則修正從來沒真的發布到 Firebase Console，
  已經一併修正，但驗證後跟這次症狀無關）。真正定位到問題的關鍵測試：**直接在手機
  瀏覽器打開 Firestore 的 REST 網址**（不透過 App），得到乾淨的 403 回應——證實這支
  手機確實連得到 Firestore 伺服器本身。
- 根因：App 用的是 Firestore SDK 預設的即時串流連線（WebChannel，一種長連線協定），
  這跟一般的 HTTPS 請求／REST API 是不同的連線方式。某些防火牆、企業或行動網路的
  中間代理設備，會放行一般網頁請求，但擋掉或干擾這種長連線——這正好解釋了「REST
  連得上、但即時同步永遠連不上」這個矛盾的症狀。
- 修法：初始化 Firestore 時加上 `experimentalAutoDetectLongPolling` 設定，讓 SDK
  自動偵測目前網路環境撐不撐得住即時串流連線，撐不住就自動改用長輪詢（HTTP
  long-polling）——這種連線方式跟一般網頁請求很像，比較不會被同樣的防火牆/代理
  擋下來。網路環境正常、串流連線本來就沒問題的使用者完全不受影響，SDK 偵測到可以
  用串流就會繼續用串流。
- 這個修法沒辦法在開發環境模擬「特定網路擋長連線」的情況來重現，是根據實測收斂
  出來的診斷結果對應到的官方建議做法，實際效果要等這支手機在同樣的網路環境下重新
  整理後才能確認是否解決。

## v2.33.14 — 2026-08-02

**即時監聽改成只看最近 30 天，避免讀取量隨歷史紀錄無限成長**

- 背景：你發現的問題——即時監聽（`onSnapshot`）原本是監聽整個 `events`／
  `growth` collection，完全沒有篩選條件，所以每次重新連線（重整頁面、下拉重新
  整理、背景喚醒重連）都會把**全部歷史紀錄**當成新的重新讀一次。這筆成本只會
  隨紀錄數持續變大：紀錄越多、每次重連要讀的量跟花的時間就跟著往上疊，沒有
  上限。
- 修法：即時監聽改成只看 `updatedAt` 在**最近 30 天**內的紀錄（`where('updatedAt',
  '>=', 30天前)`）。這樣一來，平常重新整理只需要重讀「最近有異動」的一小部分，
  不會隨歷史累積無限膨脹。超過 30 天沒人動過的舊紀錄，只要有誰去編輯它，
  `updatedAt` 就會被更新成現在，自動重新進入監聽範圍——不需要額外處理，兩邊
  裝置照樣能即時同步到這次修改。
- 這支裝置本來就有的舊資料不受影響：既有裝置本來就已經透過舊機制把完整歷史抓
  下來過，直接判定為「已完整同步過」，不會因為這次改版又重讀一次全部歷史。
- 只有「這支裝置第一次綁定這個家庭、本機完全沒有資料」的情況（真正的新裝置／
  新帳號，這正是你之前提到的例外）才需要一次性的完整歷史讀取——這次讀取用的
  是一般讀取（`get()`，分頁進行，非即時監聽），只發生一次，之後就交給上面的
  30 天即時監聽負責後續更新，不會重複發生。
- 有考慮過用同樣的「時間窗＋放棄」邏輯處理**推送**（寫入）端，但那是不同的機制、
  不同的取捨：推送端已經有水位線機制，追上進度後每次推送幾乎零成本，時間窗省不了
  什麼；真正的風險是「時間到了就放棄」會讓卡住的舊紀錄永久遺失、兩邊資料永遠對不
  起來。所以推送端維持不設時間限制，只有這次改動的是「讀取/即時監聽」的範圍，
  跟寫入端完全分開處理，沒有資料遺失風險。
- 實測驗證：即時監聽正確帶上 30 天篩選條件；既有裝置（本機已有歷史資料）不會
  觸發多餘的一次性完整讀取；全新裝置（本機無資料）正確分批讀取完整歷史並全數
  合併（測試用 1200 筆事件驗證分頁邏輯，3 頁 500+500+200 正確全部讀到）；重複
  呼叫不會啟動第二次重複的完整讀取；一次性讀取失敗時（例如剛好又碰到讀取額度
  用盡）不會誤標記為「已完成」，下次重連會自動重試，不會漏資料。

## v2.33.13 — 2026-07-31

**修復：配額用盡期間，每次重新整理都會再燒一次幾百筆寫入**

- 症狀：你在 Firebase 用量頁面連續看到好幾次「剛重新整理，馬上又跳出幾百到幾千筆
  寫入」的尖峰，即使昨天已經修過全量重推的問題。
- 根因：昨天的水位線修法（2.33.9/2.33.12）只處理了「正常情況下不要重推已經同步過
  的紀錄」，但沒處理「這次推送本身就會失敗」的情況——免付費方案的每日寫入額度
  用盡後，Firestore 會直接拒絕新的寫入（`resource-exhausted` 錯誤），而水位線只在
  **推送成功**時才會前進。結果就是：只要額度還沒恢復，**每一次重新整理都會重試同一批
  尚未成功的紀錄、然後同樣失敗**——不是新的 bug 每次發生一次，是同一個「額度用盡」
  卡在那裡，被每次重新整理重複觸發。實測用模擬的 Firestore 拒絕重現：額度用盡時，
  每次重新整理固定重推同一批（測試中約 400 筆一批，實際依當下待同步的分批而定）；
  額度恢復後才會一次補齊，之後才恢復正常。
- 你提議「只同步最近 24 小時，除非是新帳號」——這個方向可以縮小單次重推的量，
  但沒解決「持續重試」本身，而且會有個更嚴重的副作用：**超過 24 小時還沒同步成功
  的舊紀錄會被直接放棄**，不會再有第二次機會送出去——這正是現在卡住的這批資料
  （額度用盡前就已經存在、只是還沒推送成功），用時間窗會讓它們永遠同步不出去，
  兩支手機的資料就會真的對不起來。
- 改用**額度冷卻機制**：偵測到 `resource-exhausted`（額度用盡，不是網路不穩之類的
  暫時性錯誤）時，記住「這個家庭要等幾小時後才能再試」，冷卻期間內，不管是整批
  補送還是單筆即時推送，都直接跳過、不再嘗試（不浪費任何一次注定失敗的寫入嘗試）；
  資料仍然完整留在本機，冷卻時間一到自動恢復正常補送，不會遺漏任何一筆。網路不穩
  等其他非額度原因的失敗，不受影響、照樣正常重試。
- 實測驗證：額度用盡觸發冷卻 → 之後每次重新整理**完全不再嘗試**（不是失敗更少，是
  根本不再打出這個請求）；個別即時新增的紀錄在冷卻期間也正確跳過；冷卻時間過後
  自動恢復、完整補齊所有待同步紀錄；單純網路異常（非額度問題）不會誤觸發冷卻，
  照常重試。

## v2.33.12 — 2026-07-30

**針對「連續修復間互相干擾」這類根因，跑一次全面對抗式程式碼審查，一次修完 22 個問題**

- 背景：前幾輪（2.33.5～2.33.9）每次都個別測試單一修復，卻沒測出**兩個修復疊加後
  互相干擾**的狀況（例如 2.33.7 的自動重整 + 尚未修好的全量重推，疊加成無限重整
  循環）。既然這個模式已經出現過一次，不能假設只出現這一次——這次改用多代理人
  對抗式審查（多個角色各自找問題、每個發現再由 3 個獨立角色分別嘗試推翻、多數決
  才算數），專門針對「同步／登入／登出／切換家庭」這條路徑上所有生命週期交互。
  共找出 29 個候選問題，對抗驗證後確認 22 個為真，以下全部修復並逐一用真實模擬
  資料實測（不是單純看程式碼判斷），而非僅憑推論宣稱「應該修好了」。

- **（最高影響）多重家庭帳號登出會清掉裝置的家庭綁定**：`onAuthStateChanged` 的
  登出分支原本會呼叫 `Store.bindFamily(null)`，清掉這支裝置「上次在用哪個家庭」
  的記錄。只屬於單一家庭的帳號不受影響，但同時管理多個家庭（例如同時記錄自己
  小孩跟幫忙代管朋友小孩）的帳號，登出重登後會**無預警跳回第一個家庭**，而不是
  剛剛在用的那個。修法：登出分支不再清除這個綁定，成功測試「登出不清除→重新
  登入仍回到原本家庭」與「帳號不再屬於原家庭時安全退回第一個家庭」兩種情境。
- **Firestore 批次寫入超過 500 筆上限**：`_pushAllLocal()` 原本一次把整批待推
  文件塞進同一個 `batch()`，超過 Firestore 每批 500 次操作的硬上限會直接失敗。
  改成 400 筆一組分批遞交，水位線依實際送達的最大 `updatedAt` 正確推進。實測
  850 筆分成 `[400, 400, 50]` 三批送出，總數與水位線都正確。
- **快取卡住自動重整的安全機制**：`sessionStorage` 被瀏覽器政策封鎖（無痕模式、
  隱私設定）時，原本「這個 session 只重整一次」的防呆會直接失效，退化回無限
  循環。改成讀寫失敗時直接判定為「疑似網路阻擋」、放棄自動重整，寧可少做一次
  重整，也不能再讓循環重演。同時把「重整過了嗎」的旗標改成依家庭 ID 分開記錄，
  避免切換家庭時互相誤判。
- **本機儲存失敗與資料損毀不再靜默**：`localStorage` 讀寫失敗（`persist`／
  `local`／`bindFamily`／`setCaregiver`）與 JSON 損毀（`_loadFromDisk`／
  `_loadNurse`）先前都只是吞掉例外、畫面上完全看不出任何異狀。現在全部會記錄
  並回報，之後這類問題不會再無聲無息地發生。
- **監聽器生命週期修正**：`_detachListeners`（登出、切換家庭、手動重新同步都會
  觸發）現在會一併清掉「卡在快取」的警示狀態與計時器，不讓上一輪的過期狀態
  滲進下一輪；`forceResync` 的非同步流程加上世代 token，若途中又發生一次登出／
  切換家庭，過期的重新連線流程不會再誤將監聽器接回錯誤的家庭。
- **其餘中低優先度**：連線監聽器的錯誤在 800ms 內只算一次（避免同一次斷線被
  三個 collection 的回呼各算一次、提早觸發放棄重試）；個別即時推送成功後也會
  推進水位線（不然水位線只靠批次推送前進，同一批紀錄會被重複判斷成「還沒送」
  而重送）；切換家庭時重置推送失敗計數，不讓舊家庭的錯誤警示殘留到新家庭；
  頭像讀取/解碼失敗、`fetchFamilyLabel` 抓取失敗都補上錯誤記錄與提示。
- **併發保護**：`_pushAllLocal()` 加上執行中旗標，避免使用者在批次推送尚未完成
  時（例如快速重複下拉重新整理）又觸發一次，造成同一批資料重複送出。
- 實測驗證：以上 22 項全部用真實程式碼路徑（模擬 Firestore、模擬儲存失敗、模擬
  斷線）逐一驗證通過，另外重新跑過 `tools/twodevice-sync-test.html` 的 11 個
  兩裝置同步情境（新增/編輯/刪除雙向同步、離線補送、衝突以較晚時間戳勝出、
  拒絕過期資料覆蓋），確認這批修復沒有造成任何既有行為的回歸。

## v2.33.11 — 2026-07-30

**視覺調整：下拉重新整理提示框改成四角圓角**

- 原本上方是直角、只有下方是圓角（因為當初設計是「從頂部邊緣掛下來的標籤」），
  改成四個角統一 14px 圓角。
- 順便說明：下拉重新整理自 2.31.1 起就不是整頁重新載入/跳轉回首頁——只會重建
  連線、重新抓資料，畫面留在原本那一頁，這是設計如此、不是 bug。

## v2.33.10 — 2026-07-30

**修復：點時間數字要點兩次才跳出數字鍵盤**

- 症狀：記錄時間時，點一下時/分的數字，只會跳出系統文字選取框，要再點一次才會
  真的跳出數字鍵盤。
- 根因：這是我先前為了修「開 App / 切頁卡頓」加的批次重繪（`requestAnimationFrame`）
  造成的副作用。點數字觸發 `startNumEdit()` 後，原本的文字會換成一個 `<input>`
  並自動 focus + 選取內容——但這個 DOM 替換動作現在被延遲到下一個影格才執行，
  **脫離了原始點擊手勢的同步呼叫堆疊**。iOS Safari 只有在 `focus()` 是在使用者
  手勢的同一個事件處理內同步呼叫時，才會可靠地跳出鍵盤；延遲一影格後才呼叫，
  就只會顯示文字選取框，鍵盤跳不出來。
- 修法：`startNumEdit()` 改用不批次處理的即時重繪（跟週冠軍煙火 `openCelebration`
  用的是同一招），讓輸入框出現＋自動聚焦都在點擊當下同步完成。
- 實測驗證：舊行為（批次重繪）點擊當下輸入框確實還不存在，跟回報症狀吻合；
  修復後同一個 tick 內輸入框已經存在且已聚焦。

## v2.33.9 — 2026-07-30

**關鍵修復：找到用量爆表的真正原因——每次連線都重推全部歷史紀錄**

- 你在 Firebase Console 用量頁面找到的證據：Spark 方案每日 2 萬次寫入配額，用到
  **98.8%**；同一天的用量圖上，某小時寫入衝到近 1 萬次。這麼小的家庭使用量，
  不可能是正常操作造成的，一定是程式重複寫入。
- 根因：`_pushAllLocal()` 每次登入/重新連線（包含每次重新整理、每次自動重連、
  每次版本更新後重新載入）都會把裝置上**全部**事件紀錄（今天約 995 筆）用
  `merge:true` 整批重寫一次。原本的註解認為「反正內容沒變、重寫是無害的」——
  這句話對**資料正確性**沒錯，但對 **Firestore 用量計費完全是錯的**：每一次
  `.set()` 呼叫都算一次寫入用量，不會因為內容沒變就不計費。今天版本從 2.33.0
  升到 2.33.9、加上大量測試造成的反覆重新整理／重連／登入，995 筆 × 十幾次
  重連，幾下就把每日 2 萬次配額燒光——寫入配額一旦打滿，新紀錄的寫入會被
  Firestore 拒絕，這正是「cs90s203 記錄 snowy 收不到」的真正成因。
- 修法：改用**水位線機制**——每個家庭、每支裝置各自記錄「上次成功推送到哪個
  時間點」，之後每次重推只會送出 `updatedAt` **比水位線新**的紀錄。正常情況下
  （紀錄本來就已經透過即時同步個別推送過了）水位線會涵蓋全部資料，變成一次
  0 筆寫入的空操作；只有真的有累積未推送的紀錄（離線期間新增、或裝置第一次
  登入）時才會有實際寫入。
- 實測驗證：995 筆歷史資料第一次推送 → 正確送出全部 996 筆（995 事件 + 1 筆
  設定）；模擬推送成功後立刻重推 → 確認變成 **0 筆寫入**；新增 1 筆真正的新
  紀錄後重推 → 確認**只推那 1 筆**，不是全部重推。
- 配額本身會在 Firebase 每日重置週期後自動恢復，不需要手動處理；這個修復是
  避免同樣的事再發生一次。

## v2.33.8 — 2026-07-30

**修復自動重整無限循環；連不上時明講很可能是網路本身的問題**

- 2.33.7 的「卡住超過 12 秒自動整頁重新整理」有個沒想到的後果：如果卡住的真正
  原因**不是**暫時性的（例如目前的網路環境本身擋住 Firestore 連線），重新整理完
  進 App 會**立刻卡回同樣狀態**，於是每 12 秒重整一次、無限循環——使用者這端
  完全感覺不出任何變化，跟「什麼都沒發生」一模一樣，這正是 2.33.7 更新後回報
  「還是一樣」的原因。
- 修法：自動重新整理**每個瀏覽器 session 只做一次**。重整完如果還是卡在離線快取，
  不再繼續循環，改成明確顯示：「App 已經自動重新整理過一次，仍然連不上伺服器——
  這通常不是這支手機或這個 App 的問題，而是目前所在的網路本身擋住了連線（防火牆、
  VPN、特定 DNS、公共 Wi-Fi 限制等），請試著切換 Wi-Fi／行動網路」。連線恢復正常
  後這個狀態會自動清除，下次真的卡住還是會再給一次自動重整的機會。
- 診斷資訊加上 `networkLikelyBlocked`。
- 實測驗證：第一次卡住 → 觸發一次自動重整；模擬重整後又立刻卡住（持續性問題）→
  確認不會再重整第二次、正確標記為「疑似網路阻擋」、畫面顯示對應提示；連線恢復 →
  確認狀態正確清除、下次卡住又能重新獲得一次自動重整機會。

## v2.33.7 — 2026-07-30

**修復連線卡在離線快取「永遠好不了」的問題**

- 症狀（snowy 手機的診斷資訊實測證實）：`fromCacheOnly: true` 持續存在，點「重新連線
  伺服器」、甚至更新到新版本都沒用，一直卡在只讀本機快取、收不到別人新紀錄的狀態。
- 追出兩個真正的漏洞：
  1. `enablePersistence()`（離線快取）回傳的是 **Promise**，但程式碼只用同步
     `try/catch` 包它——這種寫法接不到真正的失敗原因（例如多分頁衝突），錯誤會
     整個消失，開發者主控台都看不到，更別說使用者。
  2. 2.33.5 的「重新連線伺服器」只呼叫一次 `disableNetwork/enableNetwork`，如果卡住
     的原因**不是**暫時性的（而是離線持久層本身卡死），這個按鈕會顯示「重新連線中」
     然後什麼都不會改變——沒有下一步。
- 修法：
  1. `enablePersistence()` 的失敗原因現在會被正確接住並記錄（診斷資訊新增
     `persistenceError`）。
  2. 新增自動偵測：只要連線持續停在「僅本機快取」超過 12 秒都沒有自己恢復，
     不再乾等使用者手動處理——會跳提示「連線卡住了，即將自動重新整理」，接著
     自動整頁重新整理（唯一保證能把離線持久層徹底重建的手段）。
  3. 正常情況下（連線只是短暫慢）幾秒內就會自己恢復，不會被誤判為卡住、不會
     無故自動重新整理。
- 實測驗證：卡住超過門檻 → 跳提示（確認會觸發自動復原路徑）；卡住後在門檻前自己
  恢復 → 確認不會誤觸發。

## v2.33.6 — 2026-07-30

**修復「無法登入」：email 白名單比對區分大小寫**

- 症狀：snowy 在 2.33.5 登入不了，登出重登也一樣。
- 根因：白名單比對（`js/firebase-sync.js` 的 `FAMILIES` 與 `firestore.rules` 的
  `in [...]`）都是**區分大小寫的精確字串比對**。email 本身不分大小寫，所以只要
  Google 回傳的地址大小寫跟名單裡寫的不一致，就會被判定成陌生帳號 → 進入
  「未授權」分支 → **程式主動把使用者登出**。使用者看到的就是「登入不了」：
  登進去馬上被踢出來，且訊息沒說是哪個帳號被拒，完全無從判斷。
- 修法：
  1. 前端新增 `normEmail()`（trim + 轉小寫）後再比對。
  2. `firestore.rules` 用 `request.auth.token.email.lower()` 比對，名單一律小寫。
     （**需在 Firebase Console 重新發布規則才會生效**。）
  3. 未授權訊息改成顯示**實際被拒絕的 email**，診斷資訊也會列出來，之後這類問題
     一眼就能看出是打錯字、登錯帳號還是大小寫問題。
- 實測驗證：`snowy5420@gmail.com`／`Snowy5420@gmail.com`／`SNOWY5420@GMAIL.COM`／
  前後帶空白，四種寫法都正確對應到 default 家庭；未在名單內的帳號仍正確拒絕。

## v2.33.5 — 2026-07-30

**修復即時同步失效：離線快取假裝成「同步中」，且我在 2.31.1 拆掉了自救手段**

- 症狀：2.33.4 上，A 手機加了紀錄，B 手機不會即時更新，兩支都同版本、都顯示
  「✓ 即時同步中」。
- 先做了**兩台裝置的離線模擬測試**（`tools/twodevice-sync-test.html`：兩個 iframe
  各自跑一份真實的 Store/Sync，中間接假雲端），11 項全部通過——雙向新增、編輯、
  刪除、離線補送、衝突解決、過期資料忽略都正確。這證明問題不在合併邏輯，而在
  真正的 Firestore 連線層。
- 根因兩個，互相加乘：
  1. `enablePersistence`（離線快取）開啟時，連線卡住**不會**產生錯誤——`onSnapshot`
     會安靜地改從本機 IndexedDB 快取回應，並照樣回報成功。所以畫面顯示「即時同步中」，
     實際上永遠收不到對方的新紀錄。
  2. 2.31.1 把下拉重新整理從整頁重載改成「斷開再接上監聽器」——**這不會重建底層網路
     連線**，重新接上只會再讀同一份過期快取。等於把原本唯一能救回這種狀況的手段
     （整頁重載會重建整個 SDK）拆掉了。
- 修法：
  1. 追蹤 `snapshot.metadata.fromCache`：只要資料來自快取，狀態列就改成紅字
     「⚠️ 目前讀的是離線快取，沒連上伺服器」，並明說「看不到其他人的新紀錄」，
     **不再顯示「✓ 即時同步中」**；旁邊給一顆「🔄 重新連線伺服器」按鈕。
  2. `forceResync()` 改成真的重建連線（`disableNetwork()` → `enableNetwork()` → 重新
     掛上監聽器），失敗則退回整頁重載，不會再出現「重新整理了但其實什麼都沒做」。
  3. 診斷資訊加上 `fromCacheOnly`。
- 實測驗證：正常狀態顯示「即時同步中」→ 切成僅快取後確認轉紅字警告、確認不再顯示
  「即時同步中」、重新連線按鈕出現 → 恢復伺服器連線後警告自動清除；兩台裝置模擬
  測試 11 項仍全數通過（無回歸）。

## v2.33.4 — 2026-07-29

**關鍵修復：上傳雲端失敗不再靜默——這是今天資料流失的根本成因**

- 今天下午的紀錄流失，追根究柢是這個：`pushDoc()`／`_pushAllLocal()` 上傳失敗時，
  只寫進開發者 console，**畫面完全沒有任何提示，同步狀態還繼續顯示「✓ 即時同步中」**。
  所以那批紀錄實際上只存在單一支手機、從未進雲端，卻看起來一切正常——直到後續
  一次重新載入讀到不同的儲存位置，那批沒有雲端備份的紀錄就再也找不回來。
- 修法：
  1. 每次上傳失敗都會計數並記錄錯誤，**第一次失敗立刻跳提示**。
  2. 設定 →「同步與帳號」的狀態列在有上傳失敗時改為紅色警告（顯示筆數與錯誤原因），
     **不再顯示「✓ 即時同步中」**，並提示「這些紀錄目前只存在這支手機」、建議先下載
     備份 JSON 再重試。
  3. 診斷資訊加上 `pushFailures`。
  4. 上傳成功後自動清除警告，不會留著過期的紅字。
- 實測驗證：模擬上傳失敗 → 提示跳出、狀態列轉紅色警告、確認不再顯示「即時同步中」；
  模擬恢復 → 警告自動清除、狀態列回復正常。

## v2.33.3 — 2026-07-29

**診斷資訊加上「今天的紀錄明細」**

- 回報是「部分資料不見」——今天下午原本有的紀錄，在 `?fresh=1` 之後消失，其他歷史
  資料都還在。診斷面板現在會列出今天每一筆紀錄（時間、類型、記錄者、更新時間），
  **包含被標記已刪除的**（標上「← 已刪除」）。這能直接分辨兩種完全不同的故障：
  紀錄整筆不存在（搬遷漏掉）vs 紀錄還在但被同步邏輯誤標成已刪除（可能救得回來）。

## v2.33.2 — 2026-07-29

**新增診斷資訊；修掉 v2.33.1 自己引入的跨家庭資料污染風險**

- 設定頁最下方新增「診斷資訊」，點開會列出這支手機**實際的**儲存狀態（登入帳號、
  目前綁定的家庭、正在讀哪個儲存位置、每個 `bt_*` key 各有幾筆資料）。「資料不見了」
  這種問題可能出在本機快取、家庭綁定或雲端任一層，之前完全沒辦法從手機端分辨，
  現在截圖這個畫面就能直接定位。列的是筆數/大小不是資料內容，截圖分享是安全的。
- 修掉 v2.33.1 的漏洞：那版的「新位置是空的就回頭讀舊資料」對**任何**家庭都成立，
  所以如果裝置切到還沒有資料的第二個寶寶（friendA），會把第一個寶寶的歷史紀錄
  複製進去，還會把原本的 key 刪掉。現在會記住「舊資料屬於哪個家庭」，只有那個
  家庭能繼承，其他家庭一律從空的開始（照原本設計從雲端同步下來）。
- 實測驗證：綁定第一個家庭正常繼承舊資料 → 切到第二個家庭確認是空的、沒被污染 →
  切回第一個家庭確認資料完好；v2.33.1 的自我修復行為仍正常；診斷面板輸出正確。

## v2.33.1 — 2026-07-29

**緊急修復：多寶寶搬遷邏輯可能造成資料看起來消失**

- 事故：手機用 `?fresh=1` 強制抓新版後，時間軸上一堆資料不見了。追查是 v2.30.0
  加的「多寶寶切換」搬遷邏輯有瑕疵——搬遷用的「已完成」標記，在資料**真正寫入
  成功之前**就先設定了。如果那次寫入失敗（最可能是搬遷過程中新舊資料短暫同時
  存在、逼近手機瀏覽器的儲存空間上限），標記還是被設成「完成」，但新的儲存位置
  其實是空的——下次載入，程式看到標記就不會再回頭找舊資料，畫面等於空的。
  **舊資料本身沒有被刪除，只是程式不再讀取它**。
- 修法：
  1. 判斷「這個家庭是否已經搬遷過」改成看**新儲存位置實際有沒有內容**，不再依賴
     一個可能跟現實脫鉤的旗標——這也讓卡在壞掉狀態的裝置下次載入時**自動修復**，
     不需要使用者做任何操作。
  2. 舊資料只有在新資料**確認寫入成功後**才會被清掉，失敗就留著，下次繼續重試。
  3. 本機儲存寫入失敗這件事，從原本靜默吞掉改成會跳出提示，以後不會再無聲無息
     發生、難以追查。
- 實測驗證：重現整個壞掉的情境（標記說搬完了但新位置是空的）→ 確認會自動抓回
  舊資料；完整搬遷流程 → 確認寫入成功後才清掉舊資料；模擬寫入失敗 → 確認會跳
  提示、舊資料保留、畫面顯示的資料還是對的；已經搬遷成功的正常情況 → 確認不會
  被誤動。

## v2.33.0 — 2026-07-29

**新功能：固定餵奶提醒的目標日曆，不用每次自己選**

- 設定頁新增「餵奶提醒」區塊，可以填一次「偏好日曆 ID」。填了之後，點首頁奶瓶
  icon 產生的 Google Calendar 連結會直接帶 `src=` 指到這個日曆，不用每次在
  Google Calendar 那邊自己選。
- 這個設定**只存在這支手機**（裝置本機，不隨帳號同步），每個照顧者可以各自填
  自己想固定用的日曆，不會互相覆蓋或影響到別人。留空則維持原本每次自己選的
  行為。

## v2.32.1 — 2026-07-29

**調整：預計下一餐線改成一定畫，跟 NOW 太近時左右分開避免重疊**

- 上一版「跟 NOW 太接近就不畫預計線」改成**一定畫**：NOW 線顏色/z-index 較高、
  維持視覺優先權，「預計」文字改貼到**右側**（NOW 文字維持左側），兩條線疊在
  一起時文字左右分開不會互相蓋到。

## v2.32.0 — 2026-07-29

**新功能：時間軸上加「預計下一餐」線**

- 今天時間軸上，跟橘色「NOW」線一樣，加一條「預計下一餐」線（藍色 `#79C3F0`，
  時間軸圖例裡的「尿尿」那個顏色——原本要的「刷牙」其實是綠色，用字面上的
  藍色做了，顏色不對再說一聲），標示預計下一餐的時間，**不用長按奶瓶 icon**
  就會顯示（跟長按才會出現的預測準確度覆蓋層是分開兩件事）。
- 預計時間超出畫面看得到的範圍（未來 3 小時內）就不畫，避免線跑到看不到的地方；
  預計時間跟「現在」太接近（小於 16px，例如已經過餐、快要餵了）也不畫，避免
  兩條線疊在一起看不清楚。

## v2.31.2 — 2026-07-29

**重要修復：手機可能一直卡在很舊版的程式碼，重新整理/重開 App 都沒用**

- 背景：cs90s203 記錄的資料，snowy5420 那邊過了 10 分鐘還是沒同步到，重新整理跟
  重新打開 App 都無效。追查發現 `index.html` 裡的 `<script src="js/xxx.js">` 完全
  沒有版本號，實際部署又是走 GitHub Pages + 瀏覽器自己的快取——沒有任何機制強迫
  瀏覽器在你改版後真的去抓新檔案，尤其手機「加到主畫面」的用法特別容易整個卡在
  舊版本，不管怎麼下拉重新整理或關掉重開都沒用（本次 debug 過程作者自己也踩到
  同一個坑，得手動加 cache-busting 參數才能讓瀏覽器抓到剛改的程式碼）。
- 修法：`index.html` 的每個本機 script/CSS 網址都加上 `?v=版本號` 查詢字串，以後
  **每次改版都要跟著 `APP_VERSION` 一起手動更新**（見 `index.html` 裡的
  `CACHE_VER` 註解）。網址帶版本號等於每次改版都是全新的網址，瀏覽器/CDN 快取
  再舊也一定會被強制抓新的。
- **這次要生效，兩支手機都要各自手動做一次「強制抓新版」**（因為連
  `index.html` 本身都可能被卡住，之後這個機制才會開始發揮作用）：在瀏覽器網址列
  把 App 網址後面加上 `?fresh=1`（例如 `https://xxx.github.io/babylog/?fresh=1`）
  打開一次即可，之後照平常方式使用、之後的更新就會自動抓新版，不用再做這件事。

## v2.31.1 — 2026-07-29

**效能：下拉重新整理變快，不用整頁重載**

- 下拉重新整理原本是整頁 `location.reload()`——重新下載所有 JS、重新初始化
  Firebase SDK、重新驗證登入，才會重新接上監聽器抓資料，這是最慢的做法。
  Firestore 監聽器本來就即時同步，重新整理真正需要的只是「強制斷線重連」讓
  監聽器重新跟伺服器要一次最新資料，不需要整頁重載。
- 新增 `Sync.forceResync()`：直接斷開再重新接上 events/growth/settings 三個監聽器，
  跳過重新下載程式、重新初始化 SDK、重新登入這些步驟。下拉重新整理的提示文字
  改成同步狀態變成「完成」才淡出（原本是整頁重載順便把提示一起銷毀，現在要自己
  收尾；加了 6 秒逾時保險，避免連線異常時提示卡住不消失）。
- 未登入時（沒有東西可以重新同步）維持原本 `location.reload()` 的「從頭來過」行為
  不變。

## v2.31.0 — 2026-07-29

**新功能：點首頁奶瓶 icon，加 Google Calendar 餵奶提醒**

- 首頁「預計下一餐」旁邊的奶瓶 icon，**點一下**會開新分頁，跳出 Google Calendar 的
  快速新增畫面，已經帶好標題（🍼 餵奶提醒）、預計時間、預測奶量；**長按**維持原本
  的除錯用途（切換預測準確度覆蓋層）不變，兩者不會互相干擾。
- 刻意**不指定**目標日曆（不帶 `src=` 參數）：Google Calendar 自己的新增畫面本來就有
  「選日曆」下拉選單，會列出當下登入那個帳號自己的日曆清單，讓每個照顧者各自選、
  各自的重要日曆不會被誤動，也不用在 App 裡存任何日曆 ID 或做 OAuth 串接。
- 資料不足（還沒有預測）時點下去會跳提示，不會出現壞掉的連結。

## v2.30.2 — 2026-07-27

**效能修復：開啟 App / 切換頁面卡頓、畫面跳兩三次才穩定**

- 根因：`Store.onChange` 跟 `Sync.onChange` 各自直接觸發一次全畫面重繪（整個
  `#root` innerHTML 重建）。登入同步時，光是「綁定家庭」「syncing 狀態」「events／
  growth／settings 三個 Firestore 監聽各自到達初次快照」「done 狀態」就會在不到
  1 秒內連續觸發約 5-7 次全畫面重繪——這就是「打開 App 會跳兩三次頁面才正常」
  「當日進度常常只顯示一半」的原因：使用者看到的是中間幾個資料還沒同步齊的
  過渡畫面，而不是最終結果；密集重繪也會讓剛點下去的按鈕在畫面重建瞬間「失焦」，
  造成切換頁面時卡頓約 1 秒才能點擊。單次重繪本身並不慢（實測 2700 筆事件約
  10ms），問題是短時間內疊了好幾次。
- 修法：`App.rerender()` 改成用 `requestAnimationFrame` 合併同一畫面更新週期內的
  多次觸發，只在下一影格重繪一次；同一段登入同步 burst 現在只會觸發 **1 次**
  重繪（實測驗證）。新增 `App.rerenderNow()` 給少數需要重繪後立刻讀取 DOM 的地方
  （週冠軍煙火 `openCelebration`）維持原本同步行為，避免煙火節點找不到。
- 加了一個保險：App 被切到背景時 `requestAnimationFrame` 不會觸發，若剛好有一次
  重繪卡在排隊中，切回前景時用 `visibilitychange` 強制補畫一次，避免極端情況下
  畫面卡住不動。

## v2.30.1 — 2026-07-27

**啟用切換寶寶：lunamamahappy、cs90s203 加入 friendA（測試用）**

- `js/firebase-sync.js` 的 `FAMILIES.friendA` 與 `firestore.rules` 的
  `isFriendAFamily()` 都加上 `lunamamahappy@gmail.com`、`cs90s203@gmail.com`，
  讓這兩個帳號同時屬於 `default` 與 `friendA` 兩個家庭，可以用「🔀 切換寶寶」在
  兩邊之間切換（cs90s203 是為了測試用）。
- `FAMILIES` 裡 `default` 排在 `friendA` 前面，且這兩個帳號本來就在 `default`
  名單——確認過還沒更新到新版 JS 的裝置，比對邏輯仍會先命中 `default`、行為完全
  不變；`firestore.rules` 的改動也只是新增權限，不動 `isDefaultFamily()`。
- 提醒：`firestore.rules` 需要在 Firebase Console 另外貼上發布才會生效，
  git push 只更新前端白名單。

## v2.30.0 — 2026-07-27

**新功能（beta，尚未對任何帳號啟用）：一個帳號切換多個寶寶**

- 過去一個 Google 帳號只能對應一個家庭（`js/firebase-sync.js` 的 `FAMILIES`）。現在同一
  個 email 可以同時出現在多個家庭的白名單裡，登入後在**設定 → 寶寶資料**會多一個
  「🔀 切換寶寶」按鈕——**只有帳號真的屬於多個家庭時才會啟用**，否則顯示為灰階不可按。
- 點開後列出這個帳號能存取的每個寶寶（名字/emoji 即時從對應家庭的設定讀取），點其中
  一個即可切換，目前使用中的會標示「目前」。
- 資料隔離：本機快取改成依家庭分開存放（`bt_data::{familyId}`），切換寶寶不會把兩個
  寶寶的紀錄混在一起；既有的單一家庭帳號完全無感——第一次在新版登入時，原本的
  `bt_data` 會自動搬進對應家庭的新 key，行為/資料不變。
- 這版只是把「切換」機制做出來並測試過（模擬多家庭情境驗證：資料不互相污染、切換
  UI 正確顯示/停用、標籤即時讀取），**還沒有任何帳號被實際加進第二個家庭的白名單**。
  之後要讓某個帳號真的能切換兩個寶寶，還是要照舊流程：`FAMILIES` + `firestore.rules`
  加上該 email，並在 Firebase Console 發布規則。

## v2.29.4 — 2026-07-27

**修正：統計頁「照顧者分擔」的尿布數算錯**

- 原本 🧷 尿布數是「排便＋尿尿事件數直接加總」——同一次換尿布如果排便、尿尿一起記
  （同一時間戳），就被算成 2 次；而且親餵、刷牙這些非奶類事件也被誤算進尿布數。
- 改成用跟「換尿布次數」圖表一致的定義：同一時間戳的排便＋尿尿算**一次換尿布**，
  親餵／刷牙不計入尿布數。

## v2.29.3 — 2026-07-25

**效能修復：登入同步變慢的根因**

- 找到「連線抓取過慢」的根因：Firestore 第一次 `onSnapshot` 會把整個 collection
  的每一筆都當成一個「新增」事件送來；舊版 `Store.mergeRemote()` 每一筆各自呼叫
  一次 `persist()`，而 `persist()` 會把**整份資料**重新序列化寫回 localStorage、
  並觸發一次**全畫面重繪**。資料量越大（累積越久）就越慢，是 O(n) 次全量寫入
  + 全量重繪，而不是一次。
- 新增 `Store.mergeRemoteBatch()`，同一批 snapshot 的所有筆記錄先在記憶體合併完，
  最後只 `persist()` 一次；`js/firebase-sync.js` 的 events/growth 監聽器改用這個。
- 實測 500 筆事件的初次同步：9.75 秒 → 45.8 毫秒（約快 200 倍），且合併結果
  （新增、`updatedAt` 較新覆蓋、較舊忽略）與舊邏輯一致，行為不變只是變快。

## v2.29.2 — 2026-07-22

**統計：奶量直條圖標上總和數字**

- 統計頁「奶量 ml（母乳 ＋ 配方）」圖表的每根直條上方，加上該區間的**總和 ml 小數字**，
  樣式與上方「喝奶次數」圖一致（不用點開直條就能看到總量）。
- 為了讓數字那行塞得進原本 120px 的圖表高度，直條最高從 78px 調成 64px；
  點擊直條展開母乳／配方明細的功能不變。

## v2.29.1 — 2026-07-11

**新增 baby：獨立新家庭 friendC（成員 jennifer90131@gmail.com、s95321053@gmail.com）**

- 在 `js/firebase-sync.js` 的白名單與 `firestore.rules` 都加上新家庭 `friendC`
  （兩位照顧者：jennifer90131@gmail.com、s95321053@gmail.com），資料路徑
  `families/friendC`，與現有家庭完全隔離。
- 注意：`firestore.rules` 需在 Firebase 端另外發布才會生效（git push 只會更新前端白名單）。

## v2.29.0 — 2026-07-09

**新功能：親餵（直接哺乳）計時 🤱**

- 右下角新增兩顆親餵鈕 **L / R**（設定鍵上方、不蓋導覽列，玫瑰粉色）。
    - **點一下**開始計時、**再點同一顆**結束並記錄；秒數顯示 `10s → 25m → 1h10m`。
    - 計時中**點另一顆＝換邊**（這邊暫停、那邊開始），誤觸點回來即可；結束時每邊各存一筆，
      < 10 秒的段落當誤觸丟掉。
    - 計時用「開始時間戳」計算，手機睡眠／切背景／重整／關掉都不會跑掉，重開會接續。
    - 滿 **1.5 小時自動停止**。
    - **長按 1 秒**＝補記之前忘了記的親餵（可選日期、開始時間、時長、左右、照顧者）。
    - 第一次點會跳出**操作教學**（只跳一次）；設定頁可「重置親餵教學」重看。
- **時間軸**：親餵紀錄顯示「🤱 L 10m 親餵」，長按可編輯（日期時間、時長、左右、照顧者），
  裡面也有「**繼續計時**」可接續一筆被誤停的親餵。
- **統計**：新增「親餵左右」比較（左右各自的次數、總時長，與時長佔比）。
- **預測**：親餵會**計入「下一餐時間」**的預測（親餵也是一餐）；但因為沒有 ml，瓶餵資料太少時
  首頁改顯示「**ml 資訊不足**」而不是硬給一個數字。親餵也算進照顧者冠軍的餵食次數。
- CSV 匯出、備份都含親餵紀錄。

## v2.28.2 — 2026-07-07

**新增使用者:sanan282000@gmail.com(獨立新家庭 friendB)**

- 在 `js/firebase-sync.js` 的白名單與 `firestore.rules` 都加上新家庭 `friendB`
  (成員:sanan282000@gmail.com),資料路徑 `families/friendB`,與現有家庭完全隔離。
- 注意:`firestore.rules` 需在 Firebase 端另外發布才會生效(git push 只會更新前端白名單)。

## v2.28.1 — 2026-07-07

**每週照顧冠軍煙火:接上正式自動觸發(移除測試按鈕)**

- 「照顧者分擔」卡片標題改回一般文字(移除測試用的點擊觸發)。
- 正式觸發:每週日晚上 8:00 結算,打開 app 時若「最近一次結算的那一週」這台裝置還沒慶祝過,
  就自動跳出冠軍煙火;錯過時間之後打開一樣會跳。
    - **每台裝置只跳一次**(用本機旗標記錄已慶祝到哪一週,不跨裝置同步——冠軍內容大家一樣,
      但每個人各自看得到一次)。
    - **只看最近一週**,不補歷史(久沒開也只跳最近那一次)。
    - **第一次安裝**不會回顧舊週,從下一個週日才開始。
    - 那一週**完全沒紀錄就跳過**;若資料是開啟後才同步進來,也會在同步完成時補跳一次。

## v2.28.0 — 2026-07-07

**新功能(測試中):每週照顧冠軍煙火慶祝**

- 加入「本週照顧冠軍」的 SVG 煙火慶祝 overlay:半透明遮罩蓋在 app 上(後面 app 仍隱約可見)+
  金/珊瑚色煙火 + 冠軍名字、戰績(🍼 X 次・🧷 Y 次)、週期日期,點任意處關閉。冠軍取自
  「照顧者分擔」排名(總次數,平手比餵奶次數、再平手則並列)。
- **目前為測試狀態**:暫時用「照顧者分擔」卡片標題當觸發按鈕,點一下就跳出當週冠軍慶祝,方便
  在手機上檢視動畫與內容。正式的「週日晚上 8:00 結算、打開才跳、每台裝置只跳一次」自動觸發
  邏輯之後再接上(屆時會把這個測試按鈕改回一般標題)。

## v2.27.2 — 2026-07-06

**修正：補餐加成太多會「爆量」(預測份量比平均高出很多、不實際)**

- 下一餐份量的「補餐加成」原本補不足量的 50%、最多補到半餐(平常的 1.5 倍),遇到整天偏低或
  間隔比平常長的日子,會累積出很大的加成,預測量爆高、不切實際。
- 調整:補償率 0.5 → **0.3**,上限半餐 → **15%**(平常量的 0.15 倍)。
- 另加一道天花板:資料夠多(至少 8 餐)時,最終預測量不超過**平常量的 1.2 倍**,當硬性防爆
  保險;資料還少時不套用,讓年齡參考量主導。
- 模型 A(短窗)、模型 B(全日)都保留。用多種情境測過:正常日不加、頻繁小餐但總量正常不加、
  極端低量日也只加到上限 15%,不會再爆量。

## v2.27.1 — 2026-07-06

**修正：長按按鈕時會誤觸系統的文字選取/放大鏡與選單**

- 全 app 檢查有長按/長按連續功能的按鈕:排便/尿尿/刷牙的快速紀錄鈕(長按補記)、彈框裡時間
  的＋／－(長按連續調整)。這些在 iOS 上長按時,會誤跳出文字選取的放大鏡、選取控點,或
  「拷貝／查詢」的 callout 選單,干擾長按手勢。
- 統一在所有 `<button>` 加上 `user-select:none` 與 `-webkit-touch-callout:none` 抑制,
  讓長按只觸發原本的功能。時間軸的紀錄膠囊、首頁預測圖示原本就已經有這個處理;輸入框、可
  選取的文字不受影響。

## v2.27.0 — 2026-07-06

**新功能：新增紀錄的彈框也能選「由誰處理」**

- 底下常駐按鈕長按跳出的新增彈框(記錄喝奶、補記排便/尿尿/刷牙),現在跟編輯紀錄一樣有
  「由誰處理」的照顧者選擇:上面是歷史照顧者名字膠囊、下面可以打新名字。
- 打開時預設選中「目前裝置的照顧者」,不改的話行為跟以前一樣;改成別人**只算這一筆**,不會
  變更裝置預設(之後直接快點的紀錄還是用原本的預設)。
- 補記時用「＋同時加上另一個」捷徑產生的那筆(例如同時記排便＋尿尿),會**沿用同一個照顧者**。

## v2.26.1 — 2026-07-06

**修正：統計頁滑動方向相反、滑到一半會卡住彈不回來**

- 翻頁方向改成跟手指走(像相簿):往右滑看**上一個**期間、往左滑看**下一個**,跟原本相反。
- 修掉「滑到一半畫面卡住、不會自己彈回」的 bug：手機瀏覽器有時會在滑動中途把手勢判定成捲動、
  改發 `pointercancel`(而不是 `pointerup`),原本沒處理這個事件,圖就停在半路。現在補上處理,
  這種情況會讓圖自動彈回原位。

## v2.26.0 — 2026-07-06

**改善：統計頁直條圖改成「跟手滑動」翻週；修正最高長條數字被標題蓋住**

- 統計頁本來就能左右滑翻上一週/月/年,但因為滑的時候完全沒有畫面回饋、放開手才判斷,感覺
  像滑不動、準確度也低。現在改成**跟著手指即時移動**:四張圖一起跟手左右滑,放開手若超過門檻
  就吸附切換到上/下一個期間,不足就彈回原位。
    - 加了**方向鎖**:明顯垂直的手勢會交還給頁面正常上下捲動,不再互相干擾。
    - 已經在最新一週(不能往未來)或最舊一週(沒有更早資料)時,往那個方向滑會有**邊緣阻尼**回饋,
      而不是毫無反應。
    - 翻到的週數在切到別頁再回來時會維持不變。
- **修正長條圖最高的長條、頂端數字被卡片標題蓋住的問題**:最高的長條會頂到標題、把上面的數字
  擠到看不見。把長條的最高高度降低、保留頂端空間(卡片高度不變),數字就不會再被蓋住(喝奶次數、
  奶量、換尿布、睡眠推估四張圖都一起修正)。

## v2.25.1 — 2026-07-06

**UI調整：時間軸「現在」標籤改為 NOW、靠最左、線更細**

- 文字從「now」改成大寫「NOW」。
- 標籤從最右側移到最左側。
- 現在時間線從 1px 再細一點，改成 0.8px。

## v2.25.0 — 2026-07-06

**新功能：下一餐份量預測加入「補償」——最近喝得少，會預測下一餐多喝一點**

- 以人的直覺，前面幾餐吃得少，接下來通常會多喝一點補回來。份量預測現在會把這件事算進去：
  最近一段時間如果實際喝的「總量」低於平常，就把下一餐的預測份量往上調（補不足量的 50%，
  最多補半餐的量）。
- **關鍵：用「一段時間內的總量」判斷，不是每餐大小**。所以「3 餐各 50ml」跟「1 餐 150ml」
  不會被誤判——多餐小口只要加起來夠，就不會亂加補償；真的總量偏少才補。
- 同時看兩個時間窗，取「落後比較多」的那個來補（不會兩個相加重複補償）：
    - 短窗（約 2 個平常餵奶間隔）：抓「剛剛連續幾餐吃少」。
    - 全日窗（從上一次夜間睡眠結束算起）：抓「今天整天下來一直略微落後」。全日的「一天起點」
      用夜間睡眠來錨定，不受午夜換日影響。
- **只補、不反向**：喝多了不會反過來預測下一餐少喝（對照顧沒幫助）。
- **夜間/起床第一餐不套用**：那段時間喝得少是因為在睡覺，不是該補的欠量。
- 可以透過「即測 vs 回測」觀察這個補償實際上有沒有讓預測更準。

## v2.24.0 — 2026-07-06

**新功能＋改版：時間軸上的預測顯示重新設計，並新增「即測」登記**

- **預測時間改放到實際紀錄旁邊**：之前「預測時間」是飄在時間軸上「預測時間本身的位置」，
  導致它常常飄到不相干的紀錄旁邊，讓人誤以為那個預測是屬於旁邊那筆紀錄的。現在改成把預測
  時間直接寫在「它所預測的那筆實際紀錄」自己那一列，一眼就對得上。
- **新增「即測」**：以前時間軸上的預測（現在叫「回測」）是每次打開都用「目前版本的演算法」
  重新算一次，反映不出「當下 app 真的顯示過什麼」。現在每次記錄一餐奶（且這筆是最新的一
  餐、不是補登記到過去）時，會把當下即時預測的「下一餐時間、份量」存下來，之後下一餐真的
  發生時，就能對照「即測」（當時真的顯示的預測）跟「回測」（用最新演算法回推的預測）。
    - 補登記到過去的紀錄（後面已經有更晚的餵奶）不會產生即測快照。
    - 舊資料沒有即測快照，只會顯示回測。
- **時間軸上的預測位置弱化**：原本的「┄ 預測 HH:MM」浮動文字拿掉，只留一個很淡的小虛線
  點標示「預測大概落在時間軸這個位置」，不再有會誤導的浮動文字。
- **「now」時間標籤移到線的下方**：移到時間線下方的空白區，避免剛餵完奶查看時，蓋住最新
  一餐旁邊的即測/回測文字。

## v2.23.3 — 2026-07-05

**Bug修正：跨過午夜時，預測時間會無故跳到完全不同的結果**

- 之前判斷「今天還沒喝奶」的條件只看日曆日期是否換了，導致半夜熟睡中、完全沒有新的紀錄，
  單純因為時鐘跨過 00:00，預測就會從「延續昨晚睡眠模式推算」整個跳成「用平常早上第一餐的
  時間」，兩者可能差很多。
- 改成用「這個家庭最近夜間平均睡多久」來判斷夜間睡眠是否該結束了（而不是看日曆換日），這
  樣半夜跨過午夜檢查時，只要還在正常的夜間長度內，預測就會維持穩定，不會無緣無故跳動。
- 同時把「平常早上第一餐時間」「平常睡前最後一餐時間」這兩個統計，從「看全部歷史紀錄」改
  成「只看最近 14 天」，跟原本就有的「最近幾次間隔」統計對齊，避免小朋友睡眠習慣慢慢進步
  時，這幾個數字互相脫節。
- 此修正是共用演算法（即時預測、時間軸回溯預測皆共用），兩邊都會同時受惠。

## v2.23.2 — 2026-07-05

**UI調整：時間軸上的「現在」時間線變細、文字改成 now**

- 時間軸上標示現在時間的橫線跟文字，原本比較粗、寫「現在 HH:MM」，容易跟旁邊的預測文字
  互相干擾。改成更細的線（1px）、文字改成較不搶眼的「now HH:MM」，維持在最右側。

## v2.23.1 — 2026-07-05

**UI調整：照顧者分擔長條圖，非第一名的長條顏色太淡看不到**

- 原本非第一名的長條用 `var(--track)`，這個顏色跟長條底下的軌道背景色幾乎一樣，看起來像
  沒有紀錄。改成用跟第一名同色系（金色）的淡化版本，能清楚看出「這是同一種代表有紀錄的
  顏色，只是比較淡」。

## v2.23.0 — 2026-07-05

**新功能：新增紀錄的彈窗也能選日期，方便補登記過去的紀錄**

- 「記錄喝奶」跟排便/尿尿/刷牙長按進場的「補記」彈窗，原本都只能調整時、分，沒辦法選日期
  （新增出來的紀錄一律會是「今天」）。現在這兩個彈窗都加上日期欄位，預設是今天，可以自己
  改成過去的日期，方便補登記忘記記錄的舊資料。
- 跟上一版修的「編輯」彈窗日期欄位是分開的兩個欄位，不會互相影響。用模擬資料驗證過：喝奶
  跟排便的新增流程都能正確存到指定的過去日期。

## v2.22.1 — 2026-07-05

**緊急修正：編輯舊紀錄時，日期會被悄悄改成今天**

- 找到原因：「編輯記錄」彈窗只能調整時、分，完全沒有日期欄位——存檔時是拿「今天」當底，把
  時分蓋上去，原本紀錄的日期就這樣被蓋掉了。不只是改「由誰處理」會踩到，過去只要透過這個
  彈窗改任何一筆舊紀錄（不管當時想改的是什麼），存檔後日期都已經被改成「按下儲存的那一天」。
  「同時加上另一項」（例如編輯排便時順便加尿尿）也是同樣的問題。
- 修法：編輯彈窗加上日期欄位，打開時會先帶入這筆紀錄原本的日期，存檔時改用「這個日期」＋
  時分組成完整時間，不再用「今天」當底。用模擬資料驗證過：只改由誰處理、不動日期，原本日期
  會正確保留；手動改日期欄位、或用「同時加上另一項」，也都正確運作。
- 之前已經被改壞日期的紀錄不會自動修復，需要自己手動檢查調整。

## v2.22.0 — 2026-07-05

**新功能：睡眠時數推估（統計頁 + 月曆單日總覽）**

- 不用另外手動記錄睡眠——用「前一天最後一餐」到「當天第一餐」的間隔，推估晚上大概睡了多久。
  只做夜間這段推估，不猜白天的小睡（清醒玩耍的時間隨月齡拉長，空檔不代表在睡覺，猜測準確度
  會變低，所以刻意不做）。畫面上都清楚標示「推估」，不是實際睡眠紀錄。
- 統計頁「餵養」分頁新增「睡眠時數 推估」長條圖，本週每日一根、本月每週平均、本年每月平均，
  跟現有的喝奶次數/奶量/換尿布長條圖並列。
- 統計頁「本週平均」那排數字，加入「平均睡眠(推估)」，並改成上下兩排（避免6個數字擠成一排）：
  上排是平均奶量、平均睡眠、假日規律；下排是平均間隔、平均排便、平均尿尿。
- 月曆點開單日展開時，總覽數字也改成上下兩排，新增「前一晚睡眠(推估)」。

## v2.21.0 — 2026-07-04

**新功能：月曆單日展開新增當天總覽數字**

- 紀錄頁 > 月曆 > 點開某一天，日期標題底下新增一列當天總覽：總奶量ml、喝奶次數、排便次數、
  尿尿次數、平均每餐ml，一眼看到那天的整體狀況，不用自己數時間軸上的紀錄。

## v2.20.1 — 2026-07-04

**修正：預測下一餐的時間，光是等久一點沒紀錄新東西也會突然跳掉**

- 找到原因：判斷「現在是不是該切換成夜間模式」這件事，原本是拿「你打開App當下的時間」去
  跟門檻比（現在時間有沒有超過平常最後一餐時間、距離上一餐有沒有超過平常間隔）。這代表同一
  筆資料，光是你晚一點才看，就可能從「還沒超過門檻」變成「超過了」，預測整個從傍晚時段的
  推算，瞬間跳成夜間時段的推算（通常差很多）——跟你回報的22:00看到23:30、22:40變00:40
  是同一個原因。
- 修法：把這個判斷改成完全看「上一餐本身」的時間（是不是已經接近或超過平常最後一餐的時
  間），不再參考「你現在幾點在看」。這樣一旦餵奶紀錄下去，預測結果就固定了，不管隔多久再
  打開App查看，看到的都是同一個穩定答案，不會因為晚一點看而整個跳掉。用你回報的情境模擬過，
  修正後22:00跟22:40（甚至23:50）看到的都是同一個預測時間。

## v2.20.0 — 2026-07-04

**App整體review後的5項修正**

- **同步失敗自動重試**：Firestore 監聽發生錯誤時（例如網路暫時不穩），原本會直接卡在「同步
  失敗」不會恢復，現在會自動重試（間隔逐次拉長，最多5次），只有真的重試多次都失敗才顯示
  「同步發生錯誤（已自動重試多次）」，需要手動點擊重試。
- **改「我是誰」的名字會先確認**：因為改名字會把過去所有標記成舊名字的紀錄一起改掉、沒辦法
  復原，現在改名字前會先跳出確認視窗，說明會影響哪些紀錄，取消/確定都要按了才會真的執行。
- **成長紀錄加上數值合理性檢查**：身高、體重、頭圍的輸入框原本沒有範圍檢查，打錯多一個0
  之類的狀況會直接影響成長曲線圖，現在數值明顯不合理時會擋下來、跳出提示，不會存入。
- **成長紀錄列表每一筆都顯示百分位**：原本只有「最新一筆」會顯示百分位排名，現在列表裡每
  一筆紀錄旁邊都會顯示當時的百分位（例如 P85），方便回頭看某一次量測的狀況。
- CSV匯出、JSON備份原本就已經有「已下載」的提示，這次檢查確認沒有問題，不需要修改。

## v2.19.2 — 2026-07-04

**修正：預測準確度回顧算出來的誤差是錯的**

- 找到根本原因：`predictNextFeed()` 內部一律用「現在的真實時間」去算「距離上一餐過了多久」，
  但回顧準確度（`analyzeTodayPredictionAccuracy`）是要重建「當時那個時間點『應該』預測出
  什麼」，如果你是晚上才長按查看，這個「現在」就會被錯誤地拿去當成計算基準，把明明只差10-
  20分鐘的小誤差，錯誤地判斷成「已經進入夜間模式」，硬套一個長達數小時的預測間隔，變成一
  個看起來很誇張、但根本不存在的誤差。
- 修法：讓 `predictNextFeed()` 多接受一個「假裝現在是幾點」的參數，回顧計算時傳入那一餐
  「當時的實際時間」，而不是真正的現在。用你回報的情境（首頁預測19:00，寶寶18:50喝奶）實際
  模擬過，修正後正確算出誤差-10分鐘，會落在✓準的範圍內，不再是誤差-80分鐘。

## v2.19.1 — 2026-07-04

**測試：時間軸事件色塊上也能滑動頁面**

- 事件色塊原本設定 `touch-action: none`（讓長按拖曳改時間的手勢完全交給 JS 處理），這會導致
  手指只要一開始就按在色塊上，瀏覽器就完全放棄原生滾動判斷，不管後續怎麼滑都不會滾動頁面——
  跟「長按多久才觸發拖曳」是兩回事，滾動失效在那之前、瀏覽器層級就已經決定了。
- 這次試著把色塊改成 `touch-action: pan-y`，讓手指按在色塊上也能滑動頁面。代價是：因為改
  時間的拖曳也是垂直方向、跟滾動搶同一個手勢，瀏覽器有可能在長按判定完成前，就把一開始的
  些微垂直移動當作「使用者要滾動」搶走手勢，讓「長按拖曳改時間」變得比較不可靠。
- 這是一個測試版本，麻煩你實際在手機上試試看滑動頁面順不順、拖曳改時間會不會變得比較難
  觸發——如果拖曳變得不好用，跟我說一聲，我把 `touch-action` 改回 `none`。

## v2.19.0 — 2026-07-04

**新功能：預估奶量、平均每餐奶量、「由誰處理」改成標籤選擇**

- 首頁「預計下一餐」的時間旁邊，加上預估奶量，例如「16:09・約100ml」。
- 首頁預計下一餐卡片底下，原本「喝奶/ml/排便/尿尿」四格數字，額外加一格「平均每餐ml」——
  只採用有整天完整紀錄的日子（排除今天跟開始不完整的第一天），採計全部歷史資料，不隨統計
  頁切換的週/月/年區間變動。
- 編輯紀錄彈窗的「由誰處理」，從純打字改成標籤選擇——標籤清單取自全部歷史紀錄裡出現過的
  所有名字（依使用次數排序），點一下就能把這筆紀錄改成別人登記的（例如爸爸幫媽媽補登記）；
  下面仍保留文字輸入框，第一次出現的新名字還是可以直接打字新增。

## v2.18.1 — 2026-07-04

**修正：月曆選中日子的向內漸層看不出來**

- v2.17.2 加的向內金色漸層其實有畫出來，但圓圈邊緣同時疊了一個沒有模糊、死板的實心外框
  （box-shadow 0 blur），視覺上遠比底下柔和的漸層搶眼，等於把漸層蓋過去了，看起來還是像
  一般的圓圈外框。改成拿掉那個死板外框，換成有模糊的金色光暈（並加強漸層濃度），邊緣才會
  真的呈現柔和向內淡出的效果，而不是死板的一條線。

## v2.18.0 — 2026-07-04

**UI調整：熱力色帶改成純色15分鐘格；月曆時間表新增事件類型篩選**

- 單日展開/比較模式的熱力色帶，取消原本的漸層淡出效果，改成扎實的純色色塊，格子大小從10
  分鐘改成15分鐘一格。
- 單日展開跟比較模式的時間軸上方，新增「🍼喝奶・💩排便・💧尿尿・👄刷牙」篩選按鈕，各自獨立
  開關（不是單選），只想看喝奶或只想比較排便次數之類的情境都可以直接篩選掉不想看的類型；
  篩選狀態在單日展開跟比較模式之間共用。

## v2.17.2 — 2026-07-04

**UI調整：月曆選中日子的金色圈圈加上向內漸層**

- 月曆上選中（點開展開/加入比較）的那一天，原本只有一圈金色outline，現在加了一層向內淡出
  的金色光暈——圈圈邊緣是金色，往圓心方向逐漸透明，跟底下的珊瑚色熱力圖疊在一起。

## v2.17.1 — 2026-07-04

**UI調整：珊瑚色統一常駐導覽列/月曆熱力圖；單日時間軸新增密集度熱力色帶**

- 常駐導覽列（首頁/統計/紀錄/設定）選中時的顏色，跟月曆熱力圖圓圈的顏色，都改成跟統計頁
  「喝奶次數」長條圖一致的珊瑚色（`#FF8C6B`）。其他地方（FAB圓鈕、主要按鈕、時間軸「現在」
  線、✓準標記）維持原本的金色，App 現在有兩種強調色分工。
- 單日展開／比較模式的時間軸，新增密集度熱力色帶：每10分鐘一格，用當下紀錄類型的顏色（喝奶
  米色系、排便棕色、尿尿藍色、刷牙綠色）畫一條貫穿整個時間軸寬度的色帶，同一時段紀錄越密集
  顏色越深；色帶上下邊界會淡出（用同一個顏色只調整透明度，不會換色），同一時段有多種類型時
  會自然疊加混色。
- 修正過程中的一個 bug：色帶一開始只做了剛好10分鐘高，淡出效果被壓縮在幾個像素內，看起來像
  一條死板的實心線而不是柔和的光暈——改成每個色帶畫得比實際時段更高（上下往外延伸），淡出
  才有足夠空間呈現，也讓相鄰時段的色帶能自然融合、更像真正的熱力圖。

## v2.17.0 — 2026-07-04

**紀錄頁大改版：月曆形式、單日展開、多日比較；統計頁新增假日規律洞察**

- 紀錄頁不再是流水帳列表，改成月曆形式，從第一筆紀錄的月份開始算，只能在「最早月」到
  「當月」之間翻頁。
- 每天日期數字的背景是一個熱力圖圓形色塊——顏色深淺依照當天紀錄次數的多寡，沒有紀錄的日子
  就沒有色塊；一個視覺元素同時講完「有沒有紀錄」跟「那天忙不忙」兩件事。
- 點一個有紀錄的日子，下方會展開那一天24小時的完整時間軸（照實際時鐘時間排列，不壓縮空白
  時段）。
- 新增「📊 比較」模式：最多選4天，並排比較，每一天都對準同一個時鐘刻度（例如四天的凌晨3點
  會排在同一個高度），所以能直接看出「這幾天同一個時段在幹嘛」。選的天數越多，每欄會自動
  縮減內容（完整標籤 → 只留奶量 → 只留emoji），避免擠成一團。
- 統計頁「平均奶量/日」旁邊新增一欄「假日規律」——比較週末 vs 平日「第一餐」的平均時間，看
  假日通常晚起（或早起）幾分鐘；資料量還不夠時會先顯示「資料不足」。

## v2.16.5 — 2026-07-04

**UI調整：刷牙的 emoji 換成 👄**

- 圖示、快速記錄按鈕、時間軸色點、紀錄篩選、Toast 提示等所有用到刷牙 🪥 的地方，統一換成 👄。

## v2.16.4 — 2026-07-04

**新增：預測「準確」的標記，順便解決文字互相干涉的問題**

- 誤差在 ±15分鐘以內算「預測成功」，實際那筆紀錄旁邊會改顯示綠色「✓準」標記，後面接奶量
  誤差（例如「✓準 +20ml」）；如果時間不是完全吻合、但還在±15分鐘內，也會補上分鐘數（例如
  「✓準 +8m -10ml」）。
- 這個門檻也順便解決了你在 08:30 那筆看到的文字互相干涉的問題：預測「準」的時候，代表預測
  時間點跟實際時間點本來就幾乎在同一個位置，兩邊各畫一次等於是重複資訊——現在準確的情況只
  畫「✓準」這一塊，不再另外畫容易疊在一起的虛線預測標記；只有誤差超過±15分鐘、兩個時間點
  在時間軸上有明顯差距時，才會分開顯示「誤差」文字跟另一個位置的虛線標記。

## v2.16.3 — 2026-07-04

**修正：預測時間的虛線標記被實際紀錄的色塊擋住看不到**

- 找到真正原因了：v2.16.2 新增的「預測時間虛線標記」，原本是貼齊在時間軸左邊（跟事件色塊
  同一欄，`left:軸線+14px` 到 `right:4px`）——只要那個預測時間點剛好靠近某一列有色塊的實際
  紀錄，色塊本身不透明的背景就會直接蓋在虛線標記上面，整個看不到，只剩下沒有紀錄的空白列
  才看得到（例如畫面最下面靠近「現在」的那一個）。
- 改成跟「誤差 +/-幾分鐘」文字一樣，貼在時間軸最右邊那一欄（跟色塊完全不同欄位，不會互相
  遮住），現在每一個預測時間點都能正常顯示在時間軸上該有的位置。

## v2.16.2 — 2026-07-04

**修正：預測時間標籤真的要對到時間軸上對的位置，不是只有間距問題**

- v2.16.1 只解決了「文字有沒有垂直置中」，但你反應的其實是另一個問題：那段文字寫著
  「預測 12:10」，卻是貼在實際那筆餵奶紀錄（例如09:34）的那一列旁邊——時間軸上09:34那個
  高度跟「12:10」完全對不上，看起來就像亂放的。
- 這次改成兩塊分開處理：① 實際那筆紀錄旁邊只留下「誤差 +幾分鐘、+幾ml」這種差距數字，不再
  寫預測的時鐘時間；② 另外在時間軸上「預測的那個時間點」本身的位置，畫一個虛線的小圓點跟
  「┄ 預測 12:10・100ml」文字——這樣預測點才會真的出現在時間軸上12:10的地方，跟實際發生的
  09:34分開顯示，一眼就能看出差了多久、差在哪裡。
- 如果預測的時間剛好落在時間軸上已經摺疊收起來的「⋯ 無紀錄」空白段裡，這個預測點會先隱藏
  （摺疊段本來就沒有實際比例的位置可以畫），點開那段展開後就會正常顯示。

## v2.16.1 — 2026-07-04

**修正 v2.16.0 的兩個 bug：長按誤觸文字選取、預測對照沒對齊**

- 長按首頁「預計下一餐」奶瓶圖示時，會誤觸手機的文字選取/長按選單（跟先前拖曳時間軸標籤
  遇到的同一種 iOS 問題）——補上 `-webkit-touch-callout:none`、`user-select:none`、
  `touch-action:none`，跟 `.chip` 那邊的做法一致。
- 預測對照的文字（例如「預測 12:10・100ml」）原本用固定的兩行文字從那一列的最上緣往下排，
  當那一列被時間軸的「最小間距」邏輯往下推、跟前一列擠得很近時，文字視覺上看起來會偏向前一
  列、對不到自己實際餵奶的那一列。改成跟奶瓶標籤一樣用垂直置中排版，兩者的中心點現在完全對齊
  （用滑鼠實測比對過像素座標）。

## v2.16.0 — 2026-07-04

**預測功能加強：奶量預測、準確度回顧、長按顯示對照**

- 新增「預測下一次奶量」：優先參考寶寶自己過去的紀錄（近期奶量中位數），資料還不夠多的時候
  混合一份依寶寶月齡估計的參考奶量（`predict.js` 的 `AGE_ML_REF`，跟 `who-data.js` 的 WHO
  生長曲線一樣是寫死在程式裡的公開通用參考表，不是即時上網查、也不是醫療建議），隨著寶寶自己
  的紀錄累積到約 30 次餵奶後就完全採用自己的資料，確保有考慮到「寶寶長大奶量會變多」這件事。
- 新增「預測準確度回顧」：不用手動標記——因為預測本來就是純函式（只看「這個時間點之前的所有
  紀錄」算出來），所以可以直接拿今天每一次實際餵奶的時間點，回推「如果當時要預測下一餐」會算
  出什麼結果，自動比對「預測時間 vs 實際時間」「預測奶量 vs 實際奶量」的誤差，不需要额外儲存
  任何東西。
- 首頁「預計下一餐」奶瓶圖示長按（約 0.45 秒）可以顯示/隱藏這個對照——顯示在今天的活動時間軸
  上，每一筆實際餵奶紀錄右側的空白處，會列出「預測幾點・幾ml」跟誤差（+/-幾分鐘、+/-幾ml）。
  平常收起來不佔畫面，只有想確認準不準的時候才長按看。這個對照只出現在「今天」的時間軸，本
  週/本月/本年的長條圖不受影響。
- 備註：這個誤差回顧只能看「今天」發生過的餵奶紀錄，因為我這邊看不到你手機上實際的資料——如果
  想討論演算法要怎麼調整，麻煩長按看一下數字，把明顯偏差的狀況回報給我參考。

## v2.15.1 — 2026-07-04

**緊急 Bug 修正：整個 App 打開沒畫面**

- v2.15.0 把整個 App 弄壞了——`predict.js` 跟 `views.js` 兩個檔案都宣告了同一個
  `const EARLY_HOUR`，這個專案沒有用 ES module（純 `<script>` 標籤），同一份網頁裡所有
  `<script>` 標籤其實共用同一個全域作用域，同名的 `const` 宣告兩次會直接丟出
  `SyntaxError: Identifier 'EARLY_HOUR' has already been declared`，整個 `views.js`
  當場停止解析，`render()` 等函式全部沒定義，App 開起來完全空白。
- 修法：把 `predict.js` 裡的那個常數改名成 `PREDICT_EARLY_HOUR`，避開命名衝突；順便掃過
  所有 js 檔案的頂層變數/函式命名，確認沒有其他重複。
- 這次的測試疏漏：先前驗證 v2.15.0 時，我是直接呼叫 `predictNextFeed()` 這個函式本身
  （它在 `predict.js`，比 `views.js`先載入，衝突發生前就已經定義好），沒有實際確認整個
  App 畫面有沒有正常渲染出來，才會沒抓到這個問題。以後測試會加一道「確認畫面真的有內容、
  沒有 JS 錯誤」的檢查，不能只測試單一函式。

## v2.15.0 — 2026-07-04

**新功能：預計下一餐演算法大升級——早中晚分段、首末餐錨點、資料有效性過濾**

1. **資料有效性**：算「平常模式」（各時段中位數、首末餐時間、平均奶量/次數）的時候，
   現在會排除「今天」（永遠不完整）跟「第一次使用只有半天紀錄」的那個第一天（沿用統計頁
   `validStatsDays()` 同一套規則：第一天要在早上 6 點前就開始記才算數）。決定「下一餐」還是
   照樣用今天實際最後一餐當基準，只有拿來訓練歷史模式的資料會過濾。
2. **早中晚四段分類**（取代原本的白天/夜間二分法）：早上（5-11）、下午（11-17）、晚上
   （17-23）、夜間（23-5 或跨到隔天），各自獨立算中位數間隔。預測時看現在落在哪個時段，
   用對應的中位數；如果已經超過該時段平常的間隔，或是已經過了平常的最後一餐時間（見下），
   會切換成用夜間中位數，邏輯是原本「超過白天間隔就切夜間」的推廣版，不是只有日夜兩種。
3. **首末餐錨點**：從有效資料裡算出「平常第一餐大概幾點」「平常最後一餐大概幾點」。今天
   還沒有任何一餐紀錄時（例如清晨剛醒），不再從「昨晚最後一餐＋夜間間隔」去外推（容易推
   過頭），改成直接預測「今天的平常第一餐時間」；平常最後一餐時間也拿來輔助判斷該不該提早
   切換成夜間模式。
4. **平均奶量／平均餐數**：用同一批有效資料算出「平均一天喝幾 ml」「平均一天餵幾次」，
   先算出來存在回傳結果裡備用（`avgMlPerDay`／`avgFeedsPerDay`），這次沒有直接拿來改變
   預測時間本身，也還沒有在畫面上顯示，之後想用再加。
5. 驗證：模擬「第一天下午才開始記錄（排除）＋連續 4 個完整天（規律作息）＋今天還沒餵過」
   的情境，確認正確排除無效天數、正確算出平均奶量/次數、正確用「平常第一餐時間」預測今天
   第一餐，而不是死套間隔外推；`bucketOf()` 的早/中/晚/夜分類邏輯也逐一驗證過對應正確的
   時段。

## v2.14.1 — 2026-07-03

**新功能：平均值也排除「還沒開始追蹤」的天數，只有紀錄完整的第一天才算數**

- 補充 v2.14.0：如果家人是某週三才第一次開始用這個 App 記錄，週一、週二完全沒紀錄的那幾天
  不是「寶寶那天很乖」，是根本還沒開始追蹤，這種天數也要從平均值的分母排除，不只排除還沒
  過完的今天。
- 開始追蹤的那一天（第一筆紀錄所在的日曆日），只有在第一筆紀錄的時間夠早（凌晨到早上
  6 點前開始記）才算一個完整的一天、納入分母；如果第一筆紀錄是下午才開始記，那天也視為
  不完整，一併排除。
- 修正一個實作疏漏：只調整分母（排除的天數）還不夠，分子（排便/尿尿/奶量的加總）也要
  同步排除那些被排除天數的紀錄，不然分子分母對不起來，算出來的平均反而會比正確值更高。
- 驗證：模擬「週三下午才開始記錄」的情境，確認週一週二（尚未追蹤）、週三（下午才開始，不算
  完整）都被排除在分母跟分子之外，平均排便次數算出來的數字跟手算結果一致；另外驗證「當天
  凌晨 2 點就開始記錄」的情境，那一天正確被視為完整、納入計算。

## v2.14.0 — 2026-07-03

**新功能：統計摘要改成平均每日排便/尿尿，並排除還沒過完的今天**

1. 摘要卡片的「排便次數」「尿尿次數」改成「平均排便/日」「平均尿尿/日」，四捨五入到整數，
   算法沿用平均奶量那個 `days` 分母。
2. 平均奶量／平均排便／平均尿尿三個數字的分母，現在會排除「還在進行中、還沒過完的今天」
   （只有目前正在看這一週/月/年的時候才會排除；滑到過去已經結束的週期不會排除任何一天）。
   改用日曆日期去算天數（不是用毫秒除以 86400000），避免 `to` 是「現在」這種半天時間點時
   算出奇怪的小數天數。
3. 驗證：模擬本週目前 5 個日曆天（含今天），今天/昨天/前天各有紀錄，確認分母正確排除今天
   變成 4 天，平均值算出來的數字對得上手算結果。

## v2.13.2 — 2026-07-03

**Bug 修正：畫面縮放歸位的時機點抓錯了**

- v2.13.1 把「強制縮放歸位」綁在彈窗儲存/刪除/取消關閉的時候，但實際上使用者要的是
  「填完那個數字、手指離開輸入框的當下」就要歸位，不是等到整個彈窗關掉才歸位。改成直接
  在體重/身高/頭圍這三個輸入框加上失焦（`onblur`）觸發，填完哪一格、手指一離開那一格
  就立刻強制縮放拉回 100%。
- 驗證：確認輸入框失焦的當下，viewport 設定被正確加上縮放限制，350ms 後正確恢復。

## v2.13.1 — 2026-07-03

**Bug 修正：成長彈窗填完數字後，iOS 畫面沒有自動縮放回正常大小**

- 根因：體重/身高/頭圍這三個輸入框字體是 14px，低於 iOS Safari 判斷是否要自動放大畫面
  的門檻（16px），點下去打字時畫面會被系統自動 zoom in；理論上失焦時應該自動縮回去，但
  這次失焦的原因是「彈窗被整個重新渲染關閉」而不是使用者手動點旁邊，Safari 常常抓不準這種
  時機，導致縮放卡住沒有跳回來。
- 修法：不改這三個輸入框的外觀，改成在儲存/刪除/取消關閉成長彈窗的當下，用一個常見技巧
  強制把畫面縮放拉回 100%——暫時把網頁的 viewport 設定加上 `maximum-scale=1.0` 逼 Safari
  立刻歸位，350ms 後再把設定改回原本沒有限制的樣子，使用者之後還是可以正常手動雙指縮放
  整個 App。
- 驗證：確認儲存成長紀錄的當下，viewport 設定被正確加上縮放限制；350ms 後正確恢復成原本
  的設定內容。

## v2.13.0 — 2026-07-03

**新功能：新增刷牙紀錄，導覽列弧形按鈕從 3 顆變 4 顆**

1. 新增「刷牙」🪥 事件類型，跟排便/尿尿一樣支援輕點立即記錄、長按補記時間，時間軸、紀錄
   列表（含篩選分頁）、編輯彈窗都完整支援；刷牙沒有「同時加上另一項」的配對提示（那個是
   排便/尿尿專屬的，同一片尿布常常兩者都有），編輯彈窗也不會誤顯示排便/尿尿的類型切換鈕。
   顏色統一用薄荷綠 `#5BBFA0`（時間軸圓點/圖例）與 `#9FE0D0`（導覽列按鈕），日夜主題各自
   加了 `--tBrush` 這個色塊變數。
2. 導覽列「＋」上方的弧形按鈕從 3 顆變成 4 顆（🍼💩💧🪥），先用視覺化工具做了模擬圖確認
   排列效果，跟你來回調整到均等弧形分佈、跟大圓距離也調整過，才寫進正式程式碼——弧形半徑
   54px、扇形角度 132°、每顆 30px、往下靠近大圓 23px，四顆之間角度、間距都相等。
3. 驗證：輕點刷牙立即記錄且跳出正確的提示訊息（不會誤跳「同一片尿布也有？」）；長按刷牙
   正確開啟「補記刷牙」的時間調整彈窗；紀錄分頁可以篩選出刷牙紀錄，點開編輯確認沒有排便/
   尿尿的類型切換選項。

## v2.12.0 — 2026-07-03

**新功能：預計下一餐改成有日夜感知**

- 原本「預計下一餐」是把最近 8 次餵奶間隔（不分白天晚上）取中位數去推算，半夜長時間的睡眠
  空檔會被當成一般間隔看待，容易在寶寶正常睡整晚的時候還提示「該餵了」。
- 改成比照統計頁「平均間隔(不含夜間)」同一套判斷方式：把間隔分成「同一天內」跟「跨到隔天」
  兩組，各自算中位數；如果距離上一餐已經超過平常白天的間隔中位數（代表現在應該是進入夜間
  睡眠空檔了），就改用「夜間間隔中位數」去推算下一餐時間，不會再用白天的節奏誤判。不是用
  固定時段判斷夜晚，是動態依照實際餵奶紀錄學出來的。
- 資料不夠分白天/夜間兩組時（例如記錄還很少），會自動退回原本「不分日夜」的行為，不會
  整個功能失效。
- 驗證：模擬 3 天規律白天餵奶（間隔 3 小時）＋跨日夜間空檔（間隔 12 小時）的資料，確認
  剛餵完（1 小時內）會用白天 3 小時的間隔預測；已經超過 5 小時沒餵（超過白天間隔）會正確
  切換成用夜間 12 小時的間隔預測；資料量不足以分組時，正確退回原本單一中位數的行為。

## v2.11.0 — 2026-07-03

**新功能：寶寶頭像可以換 Emoji 或上傳照片**

- 首頁右上角、設定頁「寶寶資料」卡片的頭像圓圈，原本寫死是 👶，現在點一下會跳出頭像選擇
  彈窗：可以從一排常見的可愛 emoji 選一個，或是上傳照片。
- 上傳照片會在手機端直接裁切成正方形、縮小壓縮成小縮圖（JPEG，約幾十 KB）後才存進資料庫，
  沒有另外架設 Firebase Storage 這類檔案服務——存縮圖直接放進現有的 settings 資料裡，跟寶寶
  名字/生日一樣自動同步到所有裝置，不用額外設定。照片設定後會取代 emoji 顯示；有照片時
  彈窗裡會多一顆「移除照片，改用 Emoji」的按鈕可以換回去。
- 驗證：選 emoji 正確同步更新首頁跟設定頁兩處頭像；上傳照片（測試用合成圖片）正確壓縮
  成小縮圖存進 `settings.babyPhoto`、頭像圓圈正確顯示照片；移除照片正確恢復成先前選過的
  emoji。

## v2.10.1 — 2026-07-03

**Bug 修正：改「我是…」的名字，過去的紀錄沒有跟著改**

- 根因：「我是…」只是新增紀錄當下貼在那筆記錄上的名字快照（[store.js](js/store.js) 的
  `addEvent`），不是即時參照。改名字（例如打錯字後更正）只會影響改名之後新增的紀錄，
  之前已經存在的紀錄 `by` 欄位還是寫死舊名字，導致統計頁「照顧者分擔」把新舊名字當成
  兩個不同的人分開統計。
- 修法：`Store.setCaregiver()` 改名時，自動找出資料庫裡所有標記舊名字的紀錄，一次性
  改成新名字（會同步推到 Firestore），全自動、不需要額外操作。第一次設定名字（歡迎畫面）
  沒有「舊名字」可以改，不會誤觸發。
- 驗證：先用「A」記了兩筆事件，改名成「B」後，這兩筆記錄的 `by` 欄位確認被改成
  「B」；改名後才新增的紀錄本來就直接標「B」；統計頁「照顧者分擔」正確合併成一個
  「B」，不會再分裂成兩筆。

## v2.10.0 — 2026-07-03

**新功能：成長曲線自動縮放到目前月齡、顯示目前百分位**

1. 沒有成長紀錄時，圖表維持原本完整 0-24 個月的呈現方式，不受影響。
2. 一旦有紀錄，圖表預設自動縮放 X 軸到「出生～目前月齡再多 2 個月」的範圍（而不是固定
   0-24 個月），資料點不會全部擠在左邊一小角；Y 軸（體重/身高/頭圍數值）也跟著自動縮放，
   涵蓋實際紀錄跟該區間內的 3rd/97th 百分位參考線，數值範圍抓得更貼近實際數據。圖表上方
   新增一顆切換按鈕，可以隨時切回「顯示完整 0-24 個月」比較，不喜歡縮放的話按一下就能
   還原。
3. 圖表下方新增一行文字，顯示目前選到的指標（體重/身高/頭圍）最新一筆紀錄大約落在同齡
   寶寶的第幾百分位——沿用既有的 WHO LMS 百分位計算（`whoZScore()`，本來就用在畫參考線，
   這次只是把數字實際顯示出來）。
4. 驗證：沒資料時圖表跟切換按鈕都跟舊版一致；填入寶寶生日/性別並新增一筆體重紀錄後，
   X 軸正確縮放到「目前月齡＋2」、Y 軸範圍也明顯縮小貼近實際數值；切換按鈕能正確在縮放/
   完整檢視間切換；百分位文字正確顯示計算出來的數字。

## v2.9.0 — 2026-07-03

**新功能：成長紀錄可以點擊修改／刪除**

- `Store` 新增 `updateGrowth(id, patch)`，邏輯比照事件的 `updateEvent()`（帶
  `updatedAt`，會同步推到 Firestore）。
- 統計頁「成長」分頁下方的紀錄列表，每一列現在點一下就能打開編輯——跟「紀錄」分頁的操作
  方式一致（不是長按），沿用同一個「記錄成長」彈窗，新增跟編輯共用同一個表單，只是編輯時
  會帶入原本的日期/體重/身高/頭圍，標題也會變成「編輯成長紀錄」。
- 編輯彈窗新增「刪除這筆」按鈕（比照排便/尿尿事件的編輯彈窗），會跳出確認視窗再刪除，
  避免手滑誤刪。
- 驗證：點擊列表項目正確帶入原本數值；修改後儲存確認資料庫裡的數值正確更新（同一筆
  id、`updatedAt` 有更新）；刪除流程（編輯→刪除→確認）正確把該筆從清單移除。

## v2.8.0 — 2026-07-03

**新功能：支援多家庭，朋友的寶寶資料完全獨立**

- 從單一家庭資料庫改成「email → 家庭代號」自動路由：`js/firebase-sync.js` 的 `FAMILIES`
  對照表決定每個 Google 帳號屬於哪個家庭，登入後自動讀寫對應的 `families/{id}` 路徑，
  彼此資料完全分開、互不可見。新增了 `families/friendA`，白名單是
  `phoebe790322@gmail.com`、`jumptoohigh@gmail.com`。
- `firestore.rules` 同步改成每個家庭各自一條規則、各自的 email 名單，`families/default`
  跟 `families/friendA` 之間沒有任何互相存取的規則，安全邊界在 Firestore 規則層，不是
  只靠前端判斷。
- 朋友那邊完全不用學新東西：一樣是「使用 Google 帳號登入」，登入後系統自動導去屬於他們
  的獨立資料空間，感覺不到背後其實是同一個網站在服務兩個家庭。
- 已知限制：本機快取（`localStorage`）沒有依家庭分開存放，這個設計假設每支裝置只會登入
  屬於同一個家庭的帳號——如果同一支裝置先後登入過不同家庭的帳號，快取的本機資料可能在下次
  登入時被推到錯的家庭。日常使用（各自用自己的手機）不會碰到，但如果之後要支援「同一支
  裝置切換不同家庭」，需要另外把本機快取也依家庭分開存。
- 驗證：`familyIdForEmail()` 對兩個家庭的 email 分別正確對應到 `default`／`friendA`，
  不在白名單的 email 正確回傳 null（會被導向「未授權」畫面）。

**⚠️ 需要手動操作**：這次的 Firestore 安全規則有改動，麻煩到 Firebase 主控台
**Firestore Database → Rules**，把 [firestore.rules](firestore.rules) 最新內容整份貼上
存檔，朋友才能真正登入成功。

## v2.7.0 — 2026-07-03

**新功能：統計頁圖表可左右滑動切換週/月/年、顯示日期、點擊展開奶量細節**

1. 喝奶次數、奶量 ml、換尿布次數這三張圖表現在可以左右滑動切換上一週/上一月/上一年，
   本週/本月/本年三個分頁都適用。往前可以一路滑到最早有紀錄的那個週期，不能滑到未來。
   滑動方向比照 Apple 健康 App 的週圖表：往左滑＝看更早以前，往右滑＝往現在的方向。
2. 分頁按鈕下方新增一行小字副標題，顯示目前正在看的週期（例如「6/29（一）－7/5（日）」
   或「2026年6月」），滑動切換時可以清楚知道自己滑到哪裡了；本週模式下三張圖表的星期
   標籤下方也各自加上日期（例如「一」下面多一行「6/29」）。
3. 奶量 ml 圖表現在可以點一下柱子展開細節：本週模式點下去顯示當天母乳/配方各喝了多少
   ml；本月/本年模式下一根柱子代表一整週/一整月的加總，點下去就展開那一週/那個月的
   母乳/配方加總。
4. 驗證：模擬左右滑動確認可以正確切換週期、在最早紀錄的週期跟現在的週期都會卡住不能再
   滑過去；切換本週/本月/本年分頁會重置回目前週期並收起展開的細節；點擊奶量柱子在三種
   模式下都能顯示正確的日期/週次/月份標籤跟對應的母乳配方數字。

## v2.6.0 — 2026-07-03

**Bug 修正＋新功能：首頁數字午夜沒歸零、餵奶間隔排除夜間、排泄次數分開顯示**

1. 首頁的喝奶/ml/排便/尿尿數字過了本地午夜還沒歸零——根因是判斷「今天」的 `dayKey()` 用
   `toISOString()` 抓 UTC 日期，不是手機的本地日期。台灣是 UTC+8，UTC 日期要到本地時間早上
   8 點才會跨天，所以這幾個數字實際上是本地早上 8 點才歸零，不是午夜。改成用本地
   年/月/日（`getFullYear`/`getMonth`/`getDate`）判斷，午夜就會正確歸零。
2. 統計頁「平均餵奶間隔」現在會排除夜間睡眠的空檔，不然那段長間隔會把平均值拉高、不能反映
   白天實際的餵奶節奏。排除方式不是設固定時段（例如 22:00-06:00），而是動態判斷：只要兩次
   餵奶剛好跨到不同的日曆日，那個間隔就當作「夜間空檔」跳過不算，其餘同一天內的間隔正常
   計入平均。
3. 統計頁摘要卡片的「排泄次數」（排便+尿尿相加）改成分開顯示「排便次數」「尿尿次數」兩個
   獨立數字，比較貼近一般醫院衛教常見的分開記法（尿布濕度看奶量夠不夠、排便看腸胃狀況，
   兩者意義不同不適合加總）。
4. 驗證：模擬本地日期跨天的兩筆事件（UTC 同一天但本地不同天）現在會被正確判斷成不同天；
   刻意跨日的長間隔（模擬夜間空檔）確認被排除在平均值之外，只用同一天內的間隔算出精確的
   平均數字；摘要卡片正確顯示分開的排便/尿尿次數。

## v2.5.3 — 2026-07-03

**UI 修正：換尿布次數長條圖改回排便/尿尿雙色分塊**

- 上一版把柱子改成單一紫色系漸層，但這樣看不出排便跟尿尿的比例了。改回最一開始的做法：柱子
  裡面用棕色（排便）／藍色（尿尿）兩塊實色堆疊，依照當天排便跟尿尿各自的比例分配高度，不是
  模糊的顏色漸層混合；柱子整體高度仍然代表換尿布次數，方便比較不同天的量。

## v2.5.2 — 2026-07-02

**UI 修正：底部常駐按鈕擋住頁面最下方文字**

- 三個小按鈕（弧形排在「＋」上方）每個頁面都會顯示，捲到頁面最底時會蓋住最後一張卡片的
  文字（例如統計頁換尿布次數圖表下面的圖例說明）。首頁、統計頁、紀錄頁的捲動區域底部各自
  多留了空間（首頁 +30px、統計頁 +40px、紀錄頁 +60px，依各頁面實際會被蓋住的狀況調整），
  設定頁本來就沒有這個問題，維持不變。

## v2.5.1 — 2026-07-02

**UI 修正：換尿布次數長條圖改回單色，不用雙色漸層**

- 長條的顏色從「棕色→藍色」的雙色漸層改成單一色系（淺紫到深紫）的漸層，不同天的柱子高度
  一眼就能比較出來，不會被顏色過渡分散注意力——排便/尿尿的分開數字本來就印在柱子上方了，
  柱子本身只需要清楚呈現「換了幾次」這個量就好。

## v2.5.0 — 2026-07-02

**新功能：換尿布次數改成正確的「同一時間才算同一次」演算法，長條圖同時顯示三個數字**

- 原本的「尿布更換次數」圖表其實只是排便次數 + 尿尿次數簡單相加，同一次換尿布如果排便跟
  尿尿各記一筆，會被灌水算成 2 次。改成 `dedupeDiaperChanges()`：只有事件時間**完全相同**才算
  同一次換尿布，就算只差 1 分鐘也算兩次（不是用時間軸那種寬鬆的分堆間隔，因為使用習慣是
  「每次不一定一次上完，但只要不是同一時間就是不同片尿布」）。
- 統計頁摘要卡片的「排泄次數」維持原樣（單純排便+尿尿事件總數），不跟著改成換尿布次數——
  因為那張卡片的重點是紀錄大小便本身發生幾次，換尿布次數只在下面這張圖表看。
- 長條圖每根柱子上方同時印出三個數字，各自用對應顏色：換尿布次數（一般文字色）、排便次數
  （棕色）、尿尿次數（藍色），一眼就能同時看到「換了幾次」跟「大便/尿尿各幾次」，不用切換
  畫面。
- 驗證：同一分鐘記錄的排便+尿尿算 1 次換尿布；1 分鐘後的另一筆排便正確算成獨立的第 2 次；
  當天總計 3 次換尿布、2 次排便、2 次尿尿，數字完全對得上。

## v2.4.5 — 2026-07-02

**UI 微調：導覽列三顆快速記錄按鈕再向下移 5px**

## v2.4.4 — 2026-07-02

**UI 微調：導覽列三顆快速記錄按鈕加大 15%**

- 按鈕直徑跟 emoji 字體都等比放大 15%（32px→37px），位置不變。

## v2.4.3 — 2026-07-02

**UI 微調：導覽列三顆快速記錄按鈕位置/透明度**

- 三顆按鈕整體向下移 10px，離「＋」更近。
- 拿掉透明度，改成實色顯示。
- 左右兩顆（喝奶、尿尿）各自再往外多推 5px，中間排便按鈕位置不動。

## v2.4.2 — 2026-07-02

**UI 調整：導覽列三顆快速記錄按鈕改回原本顏色、加大間距避免重疊**

- 三顆小按鈕（喝奶/排便/尿尿）改回跟原本首頁大按鈕一樣的顏色（含陰影色調），不再是統一的
  卡片色。
- 三顆按鈕半徑跟扇形角度加大（同時把按鈕本身縮小一點），相鄰按鈕之間確保留有間距，不會再
  互相重疊蓋住。

## v2.4.1 — 2026-07-02

**Bug 修正＋UI 實驗：時間軸拖曳誤觸、快速記錄按鈕搬到底部導覽列**

- 時間軸事件太容易在上下滑動畫面時被誤觸拖曳。原因是長按啟動拖曳的計時器（240ms）不會因為
  手指其實正在移動（代表是在滑頁面）就取消，時間一到還是會硬切成拖曳模式、劫走你的滑動手勢。
  修法：計時器延長到 740ms，並且加上「計時器觸發前如果手指已經明顯移動，就取消這次拖曳準備」
  的判斷，兩者一起大幅降低滑動誤觸的機率。真正進入拖曳模式的那一刻，被抓住的卡片會發光一下
  確認「選到了」。
- UI 實驗：首頁「快速記錄」大按鈕區塊暫時隱藏（程式碼保留一個開關，隨時可以切回來），改成
  三顆縮小、只留 emoji 的按鈕，用簡單的三角函數算出角度座標，弧形排列在底部導覽列中間「＋」
  按鈕的正上方，看起來像肉墊上方的三根腳趾。「＋」本身的功能（記錄成長）不變；排便/尿尿
  按鈕的長按補記時間手勢也照樣保留。
- 驗證：模擬「按下後立刻移動」的滑動手勢，確認不會誤觸拖曳；模擬「按住不動超過 740ms」的
  長按，確認正常進入拖曳模式並發光；新的三顆導覽列按鈕點擊/長按功能跟原本大按鈕行為一致。

## v2.4.0 — 2026-07-02

**新功能：統計頁新增尿布更換次數趨勢圖**

- 統計頁「餵養」分頁新增「尿布更換次數」長條圖，跟現有的「喝奶次數」「奶量」圖表同樣形式
  （依本週/本月/本年切換每日/每週/每月分組），排便跟尿尿分開用不同顏色堆疊在同一根柱子裡，
  一眼看出趨勢跟兩者的比例。
- 沿用既有的 `bucketize()` 分組函式，跟喝奶次數圖表共用同一套邏輯，不影響原本摘要卡片裡
  的「排泄次數」（那個仍然是排便+尿尿的簡單加總，跟這個新圖表算法一致，只是這次多了圖表化
  呈現跟趨勢對比）。

## v2.3.8 — 2026-07-02

**Bug 修正：紀錄很密集的日子，時間軸整點數字疊在一起、順序顛倒**

- 根因：小時格線位置的計算依賴各叢集「被往下推了多少」這個數字，但這個數字不是遞增的——
  緊密相鄰的叢集可能需要推很多，下一叢如果剛好間隔比較自然就完全不用推，兩叢之間這個「推移
  量」會突然掉回去，導致格線 Y 座標跟著往回彈，畫面上就會看到後面的整點數字疊到前面的數字
  上、順序顛倒（例如 07 疊在 05 的位置）。紀錄越密集、叢集越多的日子越容易出現。
- 修法：改成用每一叢實際落點的位置（這個位置本身保證只會往下遞增，不會跳動）當作錨點，
  在錨點之間用直線內插計算格線位置，而不是套用單一叢集的推移量做外推。用一堆錨點連成的線，
  只要每個錨點本身是遞增的，整條線就一定是遞增的，不會再出現彈回去的情況。
- 驗證：用一天 25 筆、幾乎每小時都有紀錄的高密度資料重現原本的 bug，確認修法後所有整點
  數字（00~15+）跟叢集本身的位置都嚴格遞增、不再重疊或顛倒；也重新驗證了拖曳事件調時間的
  功能沒有被這次改動影響，放開後的時間跟畫面上顯示的時間仍然完全一致。

## v2.3.7 — 2026-07-02

**Bug 修正：一開始拖曳事件，時間軸格子馬上跳一下**

- 根因：時間軸的叢集分組（同時段的多筆事件要不要疊在一起、位置在哪）計算，先前只要
  `dragId` 被設定（長按滿 240ms 正式進入拖曳）就會把「正在被拖曳的那筆」整個從分組資料裡
  剔除，改用獨立的浮動框顯示。如果它原本跟其他事件同屬一叢，抽走之後那叢的平均時間、要不要
  疊字縮寫都會重新計算一次，畫面上就會看到那叢（甚至後面被推開的叢）瞬間跳位——即使手指
  根本還沒移動。
- 修法：叢集分組的計算改成永遠用完整事件清單（含被拖曳中的那筆，它在 Store 裡的原始時間本來
  就沒變，只有畫面上的浮動框跟著手指走），只在真正畫出每張卡片的時候跳過被拖曳那張本身
  （因為浮動框已經顯示它了）。這樣同一叢裡其他事件、時間軸格線的位置完全不受拖曳影響；如果
  被拖曳的是「落單」一筆（原本自己一叢），那叢的點/時間標籤也會正確整個隱藏，不會留下孤兒
  標記。
- 驗證：3 筆事件同屬一叢時，拖曳其中一筆，剩下兩筆的位置在拖曳開始瞬間完全不變；落單一筆
  的事件被拖曳時，原本位置的點跟時間標籤正確消失，只剩浮動框跟著手指。

## v2.3.6 — 2026-07-02

**優化：拖曳調整時間的完成提示，改成卡片發光取代跳出視窗**

- 拖曳時間軸事件放開後，原本會跳出「時間已更新」提示框（1.7 秒後淡出）。改成放開的當下
  讓那張卡片本身短暫發光一下（0.9 秒），取代跳出視窗——卡片本身滑到新位置定住就已經是視覺
  上最直接的回饋，跳出視窗其實是多餘的，拿掉後操作更俐落、也不會擋住畫面。
- 這個提示純粹是裝飾性的，跟資料有沒有存進去無關（`Store.updateEvent` 在跳提示之前就已經
  完成），拿掉不影響任何功能。
- 驗證：拖曳放開後不再出現跳出視窗；卡片正確套用 0.9 秒的發光動畫並在結束後自動恢復原本
  的陰影樣式。

## v2.3.5 — 2026-07-02

**UI 微調：日期標籤位移、隱藏「現在」之後的整點標記**

- 午夜日期標籤（例如「7/2」）往右移 3px。
- 時間軸的整點標記文字（00、01、02…）只顯示到「現在」為止，超過現在的部分（未來 3 小時
  預留的空白緩衝區）不再顯示整點數字跟格線，減少那段空白區域的視覺雜訊。

## v2.3.4 — 2026-07-02

**微調＋修正：現在線緩衝空間改小、午夜日期標籤位置**

- 「現在」線下方預留的緩衝空間從 80px 調整為 40px（v2.3.3 加的那個）。
- 時間軸跨午夜時，「00」整點旁邊會多顯示日期（例如「7/2」）——原本是接在「00」後面
  一起靠右對齊，導致「00」被日期文字往左推，跟其他整點數字（01、02…）對不齊。改成日期
  獨立放在「00」左邊的欄位，「00」本身固定在跟所有其他整點數字一致的靠右位置，不再跑位。
- 驗證：跨午夜情境下所有整點數字（00~04）現在都對齊在同一個右邊界；日期標籤正確顯示在
  「00」左側而不是右側。

## v2.3.3 — 2026-07-02

**Bug 修正：時間軸底部太擠、彈窗把手變粗、拖曳收合手勢範圍太小**

- 「現在」時間線太靠近下方的顏色圖例（母乳/配方/混合/排便/尿尿），最後一筆事件、現在線、
  圖例全部擠在一起不好點按。時間軸高度計算現在會多預留 80px 的緩衝空間在「現在」線下方，
  跟圖例明顯拉開。
- 上一版（v2.3.2）把彈窗把手（sheet-handle）加大觸控熱區時，不小心把它的視覺樣式也變成一塊
  粗粗的矩形，不再是原本細細的一條線——改回原本的外觀。
- 拖曳收合彈窗的手勢，原本只有精準按在把手那一小條才會觸發，改成整張彈出卡片（白色卡片本身，
  不含背景遮罩）任何空白處按住都可以拖曳收合，不用刻意瞄準把手；按鈕、輸入框、滑桿等本身有
  互動行為的元件仍然正常運作，不會被這個手勢搶走操作權。
- 驗證：現在線跟圖例間距從幾乎貼在一起變成 100px 以上；把手外觀恢復成 4px 高細線；從彈窗
  標題等空白區域按住拖曳可以正常收合，但按在滑桿或加減按鈕上不會誤觸發收合手勢。

## v2.3.2 — 2026-07-02

**Bug 修正＋優化：拖曳時間對不上時間軸、整點文字被蓋住、時間/ml 調整方式、底部彈窗手勢**

- 拖曳時間軸事件時，黃色的即時時間標籤跟畫面上的時間軸格線對不上，放開後套用的時間也是錯的
  ——根因是上一版（v2.3.1）新增的 `yOfAdjusted()`（讓格線／現在線跟著被推開的事件群組一起
  往下位移）只套用在畫面繪製上，拖曳用的「螢幕座標→時間」反算函式仍然用沒有位移過的舊算法，
  兩者因此各算各的。修法：拖曳改用對 `yOfAdjusted()` 做數值反解（5 分鐘解析度取樣＋二分搜尋
  內插）算出時間，跟畫面繪製共用同一套邏輯，永遠一致。
- 整點時間文字（例如「23:00」）跟事件本身的時間標籤共用同一欄位，彼此靠近時會視覺重疊。改成
  整點只顯示小時數字（00、01、02…），移到最左側獨立的欄位，另外加一條淡虛線延伸到時間軸主線
  方便對位，事件時間標籤留在原本比較靠近時間點的位置，兩者不再互相蓋住。
- 拖曳事件時間軸的紀錄，不能再拖到超過「現在」——避免不小心把事件記錄到未來。
- 記錄時間的加減按鍵，原本一次跳 5 分鐘，改成一次跳 1 分鐘方便微調；長按會加速連續增減。
  時間數字、喝奶 ml 數字都可以直接點一下換成輸入框打數字，不用一直點加減。
- 記錄喝奶／編輯紀錄／補記排便尿尿／記錄成長，這幾個從畫面下方彈出的視窗，現在可以直接抓著
  上方把手往下拖曳收起，拖夠遠放手就關閉，拖不夠遠會彈回原位。
- 順手修正：多筆事件塞在同一叢集、卡片需要換行擠成兩行時，時間軸的高度計算沒有把多出來的
  那行高度算進去，可能讓最下面一叢的卡片視覺上壓到底下的顏色圖例；現在會把換行需要的額外
  高度也算進時間軸／叢集間距裡。
- 全部用模擬拖曳／長按/點擊操作驗證過：拖曳中的時間標籤跟放開後實際存進去的時間完全一致；
  拖到超出畫面範圍會自動停在「現在」；長按加減鍵確實連續觸發；點數字換輸入框、打字後正確
  套用；彈窗拖曳關閉／不夠遠不關閉的行為都正確。

## v2.3.1 — 2026-07-01

**Bug 修正：重疊卡片誤觸發、事件時間點跟時間軸格線對不起來**

- 上一版把「2 筆以上就疊卡片」改成只要 `cl.items.length > 1` 就觸發，太激進——就算兩筆事件
  空間明明夠並排顯示，也會被硬疊成一疊。改成先粗估「不疊、正常並排」需要的寬度（依事件種類
  估算每個標籤大概多寬），只有真的擠不下才切換成堆疊＋縮寫模式，空間夠的話一律正常並排、
  顯示完整文字。
- 事件的點/時間標籤位置跟小時格線對不起來（例如 22:55 的紀錄畫面上卻出現在 23:00 格線下面）
  ——根因是「避免相鄰事件的標籤互相重疊」那段防撞邏輯，會把時間相近的事件的點往下推，但小時
  格線、「現在」那條線的位置沒有套用同一個往下推的邏輯，兩者因此對不齊。修法：新增
  `yOfAdjusted()`，讓格線／現在線都套用跟事件點一致的「往下推」累積量，兩者永遠對得起來。
- 順手修正：跨午夜的「無紀錄」收合區間文字（例如「22:58–20:15」）看起來像時間倒退，現在只要
  頭尾不同一天就會各自加上日期標註消除疑慮。
- 用模擬資料驗證過：2 筆同時刻的排便＋尿尿事件正常並排顯示完整文字（不再誤疊）；3 筆混合
  事件（排便＋尿尿＋喝奶）確實放不下時才切換成堆疊縮寫；所有事件的時間標籤都正確落在其
  對應小時格線之間，順序正確不再顛倒。

## v2.3.0 — 2026-07-01

**新功能：時間軸重疊事件改成堆疊卡片，喝奶滑桿改成不觸發整頁重繪**

- 時間軸同一時刻的多筆事件，原本會換行擠在一起；現在改成像一疊卡片往右錯開堆疊，2 筆以上
  重疊時自動縮寫成「emoji ＋ 一個字」（排便＝便、尿尿＝尿，喝奶只留 emoji＋ml），保持可讀。
  點任一張卡片會立刻把它疊到最上層（`z-index` 提高），確保被壓在下面的卡片也點得到、看得到。
- 記錄喝奶的母乳/配方滑桿改到很難拖、很難拉到 0——根因跟先前修過的時間軸拖曳、彈窗動畫是
  同一類問題：滑桿的 `oninput`（每移動一點點就觸發一次）原本會呼叫 `A.set()`，導致每移動
  一點點就整個畫面重繪一次，把瀏覽器原生的滑桿拖曳手勢中斷掉，滑桿因此跟不上手指、也很難
  精準拉到底。修法比照時間軸拖曳的做法：滑桿拖曳中只直接改 App 內部狀態 + 直接更新畫面上的
  文字（不觸發整頁重繪），數值本來就正確，不需要額外的「放開才寫入」步驟；設定頁的預設奶量
  滑桿也一併套用同樣邏輯，但那邊因為要寫入 Firestore，改成「拖曳中只更新畫面文字、放開
  （`onchange`）才真正送出一次」，避免每移動一點點就打一次 Firestore。

## v2.2.2 — 2026-07-01

**Bug 修正：編輯視窗調整任何數值時反覆「重新彈出」，滑桿完全沒辦法拖**

- 根因：彈出視窗（`.sheet`）套用了 CSS 的滑入動畫（`animation: slideUp`）。App 的畫面更新
  方式是每次狀態改變就整個 `#root` 重新產生一次 HTML 塞回去，所以彈窗本身也是每次都變成
  全新的 DOM 節點——而全新節點套用到 `.sheet` 這個 class，動畫就會重新從頭播一次。時間
  加減按鈕點一下觸發一次重繪、奶量滑桿用 `oninput`（每移動一點點就觸發一次）觸發更多次，
  於是每調一次數值，視窗就整個往上滑一次動畫，看起來就像「反覆重新彈出」，滑桿因為畫面一直
  在跳動也拖不動。
- 修法：記住目前開著的是「同一個彈窗」還是「剛打開的新彈窗」（比對彈窗種類 + 正在編輯的紀錄
  id），只有真正「剛打開」才播放滑入動畫，同一個彈窗裡面調整數值造成的重繪一律不重播動畫。
  用模擬操作驗證過：時間加減按鈕連續按 5 次、奶量滑桿連續拖 6 個位置，動畫都正確被抑制，
  數值也都正確即時更新；關閉後重新打開，動畫恢復正常播放。

## v2.2.1 — 2026-07-01

**Bug 修正：兩支手機登入後看不到同一份寶寶資料**

- 根因：裝置登入 Google 帳號後，即時監聽器只會把「遠端 Firestore 已有的資料」拉回本機
  （remote → local），但裝置在登入之前／登入當下就已經存在本機的資料，從來沒有被推上去過
  （local → remote 只有「登入後新增的紀錄」才會觸發 `Store._cloudPush`）。結果兩支手機各自
  的舊資料卡在自己身上，永遠不會同步給對方，看起來就像「沒連在同一個寶寶上」。
- 修法：登入成功那一刻，額外執行一次 `Sync._pushAllLocal()`，把當下本機所有 events/
  growth/settings 整批推上 Firestore（`merge:true`，重複執行也安全），確保不管這支手機
  是不是第一次登入、本機有沒有登入前就存在的資料，都會補齊上去。

## v2.2.0 — 2026-07-01

**改善：時間軸改成固定 24+3 小時捲動視窗，拖曳改成不觸發整頁重繪**

- 活動時間軸「今天」不再是「日曆上的今天（00:00–24:00）」，改成固定的「現在往前 24 小時
  ～ 往後 3 小時」捲動視窗。舊邏輯用「小時的第幾小時（0–24 的小數）」當座標，天生沒辦法
  跨午夜——過午夜看不到前一天的紀錄，拖曳也拉不過午夜線。新座標改用「距離視窗起點幾小時」
  （純小數，可以超過 24），實際換算靠真正的 `Date` 物件運算，跨天自然正確，不需要特別處理
  午夜。空檔壓縮功能（≥3 小時無紀錄自動收合）邏輯完全不變，只是套用在新座標上。
- 拖曳事件調整時間會抖動、往左拖會消失、拖曳一開始頁面會跳回頂端——三個症狀追到同一個根因：
  舊版拖曳時，手指每移動一像素就呼叫一次 `Store.updateEvent()`，觸發整個 App 重新渲染一次
  （`#root.innerHTML` 整個換掉）。畫面重繪那麼高頻，除了卡頓，捲動區域的 DOM 節點也被整個
  換成新的（重置捲動位置），拖曳用來換算座標的參考節點也可能抓到舊的/已卸載的節點，算出無效
  時間讓事件消失。修法：拖曳中只直接操作那顆事件標籤自己的 DOM 位置/文字（不寫入 Store、不
  觸發重繪），放開手指才真正寫入一次資料。額外加上：`render()` 現在會在整頁重繪前後保存/還原
  捲動位置，就算之後別的地方又不小心觸發重繪，也不會再無故跳回頂端。

## v2.1.0 — 2026-07-01

**新功能：下拉重新整理（純重整頁面，跟同步無關）**

- Firebase 版拿掉了舊版下拉同步手勢後，重新加回一個下拉重新整理功能——但這次純粹是重新載入
  整個頁面（`location.reload()`），不再嘗試觸發任何同步邏輯（Firestore 本來就即時同步，
  不需要靠這個手勢），單純給需要「重新開始乾淨狀態」時用（開發測試、或裝置的監聽器卡住時）
- 下拉時會有指示條浮現（「↓ 下拉重新整理」→ 拉超過門檻變「↓ 放開重新整理」），放開後短暫
  顯示「重新整理中…」再重新載入
- 指示條元素刻意掛在 `#root` 外面（直接接在 `body` 下），因為 `#root` 每次畫面重繪都會被
  整個換掉，如果指示條掛在裡面，手勢做到一半重繪就會把它砍掉重建

## v2.0.2 — 2026-07-01

**Bug 修正：iOS Safari 日期輸入框溢出卡片邊界**

- `<input type="date">` 在 iOS Safari 上會忽略 CSS `width:100%`，用自己內部的偏好寬度
  渲染，導致設定頁生日框、CSV 匯出「從/到」日期框都超出卡片邊界（Chrome 桌機版重現不出來，
  靠使用者提供的實機截圖才確認根因）
- 加上 `-webkit-appearance: none` 把版面控制權還給我們自己的 CSS，原生日期選擇器點擊後
  仍正常彈出

## v2.0.1 — 2026-07-01

**Bug 修正 + 改善：行動裝置手勢衝突、＋按鈕配色對齊規範**

- 長按活動時間軸的事件標籤會被 iOS 原生的選字/放大鏡搶走手勢，導致長按拖曳失效
  （`-webkit-touch-callout: none` + `user-select: none`）
- 下拉重新整理仍會誤觸發 Safari 原生的整頁重新整理：真正在捲動的元素是 `.ns`，不是
  `body`，`overscroll-behavior: contain` 要加在 `.ns` 上才擋得住
- 中間 ＋（記錄成長）按鈕改用設計規範指定的日/夜間專屬配色（`#b78d52` /
  `#9d8c6c`），不再沿用主色金黃

## v2.0.0 — 2026-07-01

**新功能：Firebase Google 登入 + Firestore 即時同步，取代 GitHub Token 方案**

- 拿掉「每支手機手動貼 GitHub token」的同步方式，改成 Google 帳號登入（Firestore
  Security Rules 白名單限制哪些帳號能存取）
- events / growth / settings 改成 Firestore 逐筆文件即時監聽（`onSnapshot`），不再需要
  下拉重新整理——換裝置幾秒內自動同步
- 拿掉舊的 pull-merge-push-retry 整包 JSON 合併邏輯，改成單筆文件 `updatedAt` 比較合併
- 設定頁「同步與帳號」改成登入/登出卡片
- 新增「下載備份 JSON」手動備份按鈕（Firestore 沒有 GitHub commit 那樣的版本歷史）

## v1.1.1 — 2026-07-01

**Bug 修正：時間軸拖曳手勢誤觸發整頁重繪，吃掉所有按鈕點擊**

- `main.js` 的全域 `pointerup` 監聽器不管有沒有真的在拖曳都會呼叫 `dragEnd()`，而
  `dragEnd()` 無條件觸發整頁重繪——重繪發生在瀏覽器「放開→觸發 click」中間，導致使用者
  點的按鈕在 click 真正觸發前就被砍掉重建，點擊憑空消失。修法：沒有拖曳動作時直接 return，
  不觸碰狀態、不重繪。

## v1.1.0 — 2026-06-30

**改善：行動裝置版面問題**

- `data-theme` 從 `.app` 移到 `<html>`，修正 CSS 變數（`--bg2` 等）無法往上傳給
  `body`/`#root` 造成的白底露出
- `100vh` 改用 `100dvh`（+ fallback），修正手機瀏覽器網址列伸縮造成的版面裁切/跳動
- 首次載入加一段內嵌 script 提前設定主題，避免白色閃一下才變深色（FOUC）
- 下拉同步／手動同步在還沒設定 token 時，改成溫和提示引導去設定頁，不再顯示嚇人的紅色
  失敗橫幅
- 設定頁「從/到」日期匯出範圍改成上下排列，修正並排時在窄螢幕手機上溢出卡片的問題

## v1.0.2 — 2026-06-30

**Bug 修正：無法用 `file://` 直接開啟，也無法在 Arc/Chrome 真實點擊互動**

- 拿掉 ES module（`type="module"`），全部改成普通 `<script src>`，`file://` 雙擊打開
  也能跑，不必透過本機伺服器
- 找到並修掉「你是誰呢」歡迎視窗打不開的根因：輸入框的 `onchange` 會觸發整頁重繪，重繪
  發生在使用者點下一步按鈕的手勢中間，導致點擊事件送不到已經被替換掉的按鈕節點上——這個
  bug 後來才發現其實跟 v1.1.1 是同一類問題，只是這次先在單一輸入框上修掉

## v1.0.1 — 2026-06-30

**改善：專案資料夾路徑排除疑慮**

- 專案資料夾名稱從 `baby log`（含空白）改為 `babylog`，排除路徑空白造成 `file://` 讀取
  問題的可能性（後來證實真正原因是 v1.0.2 記載的重繪問題，但保留這個改動）

## v1.0.0 — 2026-06-30

**初始版本**

- 首頁：快速記錄喝奶（母乳＋配方混合）／排便／尿尿，下一餐預測（最近 N 餐間隔中位數）
- 活動時間軸：今天／本週／本月／本年，長按拖曳調整時間，自動收合長空檔
- 統計：餵養摘要、照顧者分擔排行、WHO LMS 成長百分位曲線（體重/身高/頭圍，0–24 月，取自
  CDC 代管的官方 WHO 資料）
- 紀錄列表、編輯、刪除（軟刪除墓碑機制）
- 設定：寶寶資料、本機照顧者身分、日夜主題、事件時長、預設奶量、Alarm 微調
- CSV 匯出（Google 日曆匯入格式）
- GitHub Contents API 同步（pull-merge-push + SHA 衝突重試）+ 每日備份快照（v2.0.0 已換掉）
- 部署到 GitHub Pages：https://cs90s203.github.io/babylog/
